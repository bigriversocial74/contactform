# Foundation branch synchronization

This branch must include the current `main` branch before merge so the existing browser-validation foundation and the new visual-regression foundation run together.
