02G smoke test after SQL import: 1. /api/health.php returns database connected. 2. SELECT code FROM user_models WHERE code='sales'; returns one row. 3. SELECT slug FROM roles WHERE slug='sales'; returns one row. 4. SELECT slug FROM permissions WHERE slug LIKE 'sales.%' OR slug='crm.analytics.view'; returns the expected permission rows. 5. SHOW TABLES LIKE 'crm_%'; shows crm_leads, crm_lead_assignments, crm_lead_events, crm_lead_notes, crm_daily_region_stats. 6. SHOW TABLES LIKE 'sales_%'; shows sales_profiles and sales_roster. 7. SHOW TABLES LIKE 'website_analytics_events'; returns one row. 8. No existing users receive sales automatically unless manually assigned by admin later.