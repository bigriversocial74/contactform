# Stage 5B Release Notes

Stage 5B exposes the existing catalog, builder, publishing, immutable version, PPPM template, and asset systems through the shared merchant workspace.

The release adds merchant-facing product search, status filters, product detail sheets, version history, published media inspection, media-library filtering, builder deep links, publish/archive controls, onboarding synchronization, responsive product views, regression tests, and dedicated CI.
