# Stage 5F Operator Notes

Run Stage 4F aggregation and snapshot jobs before expecting populated merchant dashboards. Forecasts reuse the existing Stage 4F models. Export jobs remain asynchronous and privacy-safe. Saved reports store aggregate view definitions; scheduled report email addresses are stored only as keyed hashes.
