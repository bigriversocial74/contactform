# Archived recovery copy — do not use

This nested directory is retained only as historical recovery material.

It is not an active runtime source, deployment root, migration source, test source, or GitHub Actions source. The canonical Microgifter implementation is at the repository root of `bigriversocial74/contactform`.

Do not make new feature changes here. Do not copy workflows, Composer configuration, migrations, or application files from this directory into production without comparing them to the active root first.

Current work must use the root directories:

```text
api/
assets/
config/
database/
docs/
includes/
scripts/
tests/
.github/workflows/
```

The active source map is `docs/architecture/current_active_file_map.md`.
