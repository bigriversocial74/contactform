name: Engagement Mutations Foundation Validation

on:
  push:
    branches: [production-engagement-mutations-foundation]
  pull_request:
    branches: [main]
    paths:
      - 'profile.php'
      - 'api/social/_engagement.php'
      - 'api/social/relationship.php'
      - 'api/social/engage.php'
      - 'api/public/profile.php'
      - 'api/public/post-engagement.php'
      - 'api/tips/_engagement.php'
      - 'api/tips/create.php'
      - 'api/tips/confirm.php'
      - 'assets/js/public-profile-engagement.js'
      - 'assets/css/public-profile-engagement.css'
      - 'database/stage_18e_engagement_mutations.sql'
      - 'scripts/build_full_upgrade_sql.php'
      - 'scripts/validate_engagement_mutations_behavior.php'
      - 'tests/phpunit/ProductionEngagementMutationsFoundationTest.php'
      - 'tests/browser/engagement-mutations-foundation.spec.js'
      - 'docs/production/engagement-mutations-foundation.md'
      - 'composer.json'
      - '.github/workflows/engagement-mutations-validation.yml'

permissions:
  contents: read
  statuses: write

concurrency:
  group: engagement-mutations-validation-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: microgifter_test
          MYSQL_USER: microgifter_test
          MYSQL_PASSWORD: microgifter_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=12
    env:
      MG_TEST_BASE_URL: http://127.0.0.1:8080
      MG_TEST_SKIP_AUTHENTICATED: '1'
      MG_RUN_ENGAGEMENT_MUTATIONS_BEHAVIOR: '1'
      MG_APP_ENV: testing
      MG_DEBUG: '0'
      MG_BASE_URL: http://127.0.0.1:8080
      MG_SESSION_NAME: mg_engagement_mutations_test_session
      MG_DB_HOST: 127.0.0.1
      MG_DB_NAME: microgifter_test
      MG_DB_USER: microgifter_test
      MG_DB_PASS: microgifter_test
      MG_DB_CHARSET: utf8mb4
      MG_PAYMENT_PROVIDER: sandbox
      MG_PAYMENT_MODE: test
      MG_PAYMENT_WEBHOOK_SECRET: validation-payment-secret
      MG_PAYMENT_SANDBOX_SECRET: validation-sandbox-secret
      MG_CLAIM_CODE_PEPPER: validation-claim-code-pepper
      MG_MEDIA_SIGNING_SECRET: validation-media-secret
      MG_DISTRIBUTION_HASH_SECRET: validation-distribution-secret
      MG_DISTRIBUTION_WEBHOOK_SECRET: validation-distribution-webhook-secret
      MG_MERCHANT_INVITE_SECRET: validation-merchant-secret
      MG_REPORT_EMAIL_SECRET: validation-report-secret
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: curl,fileinfo,json,mbstring,pdo,pdo_mysql
          coverage: none
          tools: composer
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: composer install --no-interaction --prefer-dist --no-progress
      - run: npm install --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - name: Validate changed syntax
        run: |
          php -l profile.php
          php -l api/social/_engagement.php
          php -l api/social/relationship.php
          php -l api/social/engage.php
          php -l api/public/profile.php
          php -l api/public/post-engagement.php
          php -l api/tips/_engagement.php
          php -l api/tips/create.php
          php -l api/tips/confirm.php
          php -l scripts/validate_engagement_mutations_behavior.php
          php -l tests/phpunit/ProductionEngagementMutationsFoundationTest.php
          node --check assets/js/public-profile-engagement.js
          node --check tests/browser/engagement-mutations-foundation.spec.js
      - name: Configure MySQL schema prerequisites
        run: mysql -h 127.0.0.1 -uroot -proot -e "SET GLOBAL log_bin_trust_function_creators = 1;"
      - name: Apply complete ordered schema
        run: |
          mkdir -p build
          php scripts/build_full_upgrade_sql.php build/microgifter_full_upgrade.sql
          set +e
          mysql -h 127.0.0.1 -umicrogifter_test -pmicrogifter_test microgifter_test < build/microgifter_full_upgrade.sql > build/schema.log 2>&1
          status=$?
          cat build/schema.log
          exit "$status"
      - name: Validate engagement mutation behavior
        run: |
          set +e
          composer test-engagement-mutations-behavior > build/engagement-mutations-behavior.log 2>&1
          status=$?
          cat build/engagement-mutations-behavior.log
          exit "$status"
      - name: Validate engagement mutation authority contracts
        run: |
          set +e
          vendor/bin/phpunit --configuration phpunit.xml.dist --filter ProductionEngagementMutationsFoundationTest > build/engagement-mutations-contracts.log 2>&1
          status=$?
          cat build/engagement-mutations-contracts.log
          exit "$status"
      - name: Validate frontend contracts
        run: composer test-frontend-contracts
      - name: Start PHP test server
        run: |
          php -S 127.0.0.1:8080 -t . > /tmp/microgifter-engagement-server.log 2>&1 &
          echo $! > /tmp/microgifter-engagement-server.pid
          for attempt in {1..20}; do
            curl --fail --silent --show-error http://127.0.0.1:8080/api/health.php >/tmp/microgifter-engagement-health.json && exit 0
            sleep 1
          done
          cat /tmp/microgifter-engagement-server.log
          exit 1
      - name: Validate repository PHPUnit suite
        run: composer test
      - name: Validate focused browser behavior
        run: npx playwright test tests/browser/engagement-mutations-foundation.spec.js
      - name: Validate browser regression suite
        run: npm run test:browser
      - name: Show PHP server log on failure
        if: failure()
        run: cat /tmp/microgifter-engagement-server.log || true
      - name: Stop PHP test server
        if: always()
        run: if [ -f /tmp/microgifter-engagement-server.pid ]; then kill "$(cat /tmp/microgifter-engagement-server.pid)" || true; fi
      - name: Upload engagement diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: engagement-mutations-diagnostics
          path: |
            build/schema.log
            build/engagement-mutations-behavior.log
            build/engagement-mutations-contracts.log
            playwright-report
            test-results
          retention-days: 7
          if-no-files-found: warn
      - name: Publish branch validation status
        if: ${{ always() && github.event_name == 'push' }}
        uses: actions/github-script@v7
        with:
          script: |
            const success = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: success ? 'success' : 'failure',
              context: 'engagement-mutations-branch-validation',
              target_url: `${process.env.GITHUB_SERVER_URL}/${context.repo.owner}/${context.repo.repo}/actions/runs/${process.env.GITHUB_RUN_ID}`,
              description: success ? 'Engagement mutations validation passed.' : 'Engagement mutations validation failed.'
            });
