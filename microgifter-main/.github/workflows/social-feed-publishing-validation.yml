name: Social Feed Publishing Validation

on:
  push:
    branches: [production-social-feed-post-publishing-foundation]
  pull_request:
    branches: [main]
    paths:
      - 'feed.php'
      - 'api/public/feed.php'
      - 'api/social/_publishing.php'
      - 'api/social/posts.php'
      - 'api/social/report.php'
      - 'assets/js/social-feed.js'
      - 'assets/css/social-feed.css'
      - 'scripts/validate_social_feed_publishing_behavior.php'
      - 'tests/phpunit/ProductionSocialFeedPostPublishingFoundationTest.php'
      - 'tests/browser/social-feed-post-publishing-foundation.spec.js'
      - 'docs/production/social-feed-post-publishing-foundation.md'
      - 'composer.json'
      - '.github/workflows/social-feed-publishing-validation.yml'

permissions:
  contents: read
  statuses: write

concurrency:
  group: social-feed-publishing-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: microgifter_test
          MYSQL_USER: microgifter_test
          MYSQL_PASSWORD: microgifter_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=12
    env:
      MG_TEST_BASE_URL: http://127.0.0.1:8080
      MG_TEST_SKIP_AUTHENTICATED: '1'
      MG_RUN_SOCIAL_FEED_PUBLISHING_BEHAVIOR: '1'
      MG_APP_ENV: testing
      MG_DEBUG: '0'
      MG_BASE_URL: http://127.0.0.1:8080
      MG_SESSION_NAME: mg_social_feed_test_session
      MG_DB_HOST: 127.0.0.1
      MG_DB_NAME: microgifter_test
      MG_DB_USER: microgifter_test
      MG_DB_PASS: microgifter_test
      MG_DB_CHARSET: utf8mb4
      MG_PAYMENT_PROVIDER: sandbox
      MG_PAYMENT_MODE: test
      MG_PAYMENT_WEBHOOK_SECRET: validation-payment-secret
      MG_PAYMENT_SANDBOX_SECRET: validation-sandbox-secret
      MG_CLAIM_CODE_PEPPER: validation-claim-code-pepper
      MG_MEDIA_SIGNING_SECRET: validation-media-secret
      MG_DISTRIBUTION_HASH_SECRET: validation-distribution-secret
      MG_DISTRIBUTION_WEBHOOK_SECRET: validation-distribution-webhook-secret
      MG_MERCHANT_INVITE_SECRET: validation-merchant-secret
      MG_REPORT_EMAIL_SECRET: validation-report-secret
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: curl,fileinfo,json,mbstring,pdo,pdo_mysql
          coverage: none
          tools: composer
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: composer install --no-interaction --prefer-dist --no-progress
      - run: npm install --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - name: Validate changed syntax
        run: |
          php -l feed.php
          php -l api/public/feed.php
          php -l api/social/_publishing.php
          php -l api/social/posts.php
          php -l api/social/report.php
          php -l scripts/validate_social_feed_publishing_behavior.php
          php -l tests/phpunit/ProductionSocialFeedPostPublishingFoundationTest.php
          node --check assets/js/social-feed.js
          node --check tests/browser/social-feed-post-publishing-foundation.spec.js
      - name: Configure MySQL schema prerequisites
        run: mysql -h 127.0.0.1 -uroot -proot -e "SET GLOBAL log_bin_trust_function_creators = 1;"
      - name: Apply complete ordered schema
        run: |
          mkdir -p build
          php scripts/build_full_upgrade_sql.php build/microgifter_full_upgrade.sql
          set +e
          mysql -h 127.0.0.1 -umicrogifter_test -pmicrogifter_test microgifter_test < build/microgifter_full_upgrade.sql > build/schema.log 2>&1
          status=$?
          cat build/schema.log
          exit "$status"
      - name: Validate social feed and publishing behavior
        run: |
          set +e
          composer test-social-feed-publishing-behavior > build/social-feed-behavior.log 2>&1
          status=$?
          cat build/social-feed-behavior.log
          exit "$status"
      - name: Validate social feed authority contracts
        run: |
          set +e
          vendor/bin/phpunit --configuration phpunit.xml.dist --filter ProductionSocialFeedPostPublishingFoundationTest > build/social-feed-contracts.log 2>&1
          status=$?
          cat build/social-feed-contracts.log
          exit "$status"
      - name: Validate frontend contracts
        run: composer test-frontend-contracts
      - name: Start PHP test server
        run: |
          php -S 127.0.0.1:8080 -t . > /tmp/microgifter-social-feed-server.log 2>&1 &
          echo $! > /tmp/microgifter-social-feed-server.pid
          for attempt in {1..20}; do
            curl --fail --silent --show-error http://127.0.0.1:8080/api/health.php >/tmp/microgifter-social-feed-health.json && exit 0
            sleep 1
          done
          cat /tmp/microgifter-social-feed-server.log
          exit 1
      - name: Validate repository PHPUnit suite
        run: composer test
      - name: Validate focused browser behavior
        run: npx playwright test tests/browser/social-feed-post-publishing-foundation.spec.js
      - name: Validate browser regression suite
        run: npm run test:browser
      - name: Show PHP server log on failure
        if: failure()
        run: cat /tmp/microgifter-social-feed-server.log || true
      - name: Stop PHP test server
        if: always()
        run: if [ -f /tmp/microgifter-social-feed-server.pid ]; then kill "$(cat /tmp/microgifter-social-feed-server.pid)" || true; fi
      - name: Upload social feed diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: social-feed-publishing-diagnostics
          path: |
            build/schema.log
            build/social-feed-behavior.log
            build/social-feed-contracts.log
            playwright-report
            test-results
          retention-days: 7
          if-no-files-found: warn
      - name: Publish branch validation status
        if: ${{ always() && github.event_name == 'push' }}
        uses: actions/github-script@v7
        with:
          script: |
            const success = '${{ job.status }}' === 'success';
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: success ? 'success' : 'failure',
              context: 'social-feed-publishing-branch-validation',
              target_url: `${process.env.GITHUB_SERVER_URL}/${context.repo.owner}/${context.repo.repo}/actions/runs/${process.env.GITHUB_RUN_ID}`,
              description: success ? 'Social feed publishing validation passed.' : 'Social feed publishing validation failed.'
            });
