name: Main Regression (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic main validation now runs through .github/workflows/pr-validation.yml so PR and main use the same contract."
