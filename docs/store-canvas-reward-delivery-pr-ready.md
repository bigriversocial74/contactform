# PR Ready

Store Canvas reward delivery is ready for pull request validation.

The build adds merchant avatar reward sending, campaign wallet delivery, customer notification, Store Canvas session events, and source metadata display.
