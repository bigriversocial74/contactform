# PR 483 page break investigation

PR 483 is being isolated because it changed the Target Zone campaign helper and the core Target Drops CSS.

Safe action:
- Restore the Target Zone campaign helper to the pre-483 version.
- Restore the Target Drops CSS to the pre-483 version.

SQL: No SQL required.
