# Participant Quest Operating Flow

Participant: discover → sign in → pass audience gate → join → submit verified evidence → merchant review when required → reward issued → wallet.

Merchant: create Loyalty Quest → configure verification → publish → generate signed codes when applicable → review evidence → monitor rewards and redemptions.
