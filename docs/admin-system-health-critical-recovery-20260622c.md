# Admin health recovery overview

This branch fixes the production System Health critical state by recognizing the release trigger migration as manifest coverage and by making optional admin queues fail quiet when their backing tables are not installed yet.
