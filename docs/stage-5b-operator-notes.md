# Stage 5B Operator Notes

Product creation and editing continue through `/build.php`. The merchant product list and detail sheets are operating views over the existing catalog. Publishing creates a new immutable product version and PPPM template. Archiving retires active templates without deleting historical records. Media readiness is controlled by the existing upload and processing pipeline.
