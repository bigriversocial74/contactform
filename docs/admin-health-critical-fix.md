# Admin health critical fix

This branch fixes the admin System Health critical state.

It adds release trigger migration coverage and guards optional admin queues when their tables are not available.
