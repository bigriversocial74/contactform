# World Target cleanup

- Use campaign selection as the visible control.
- Hide duplicate title, payload, quantity, and claim limit controls.
- Keep hidden values in the form for existing APIs.
- Auto-save sidebar changes through the existing Target Drops API.
- Prefer attached template inventory for launch checks.
- Guard optional columns before querying.

SQL: No SQL required.
