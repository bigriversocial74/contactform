# Branch note

This feature branch was started from the Store Canvas foundation merge commit and may show as behind `main` if additional PRs merge while Phase 2 is being built. The files changed here are focused on:

- Store Canvas message delivery
- existing Messages/Notifications source metadata
- IN/OUT Box campaign reward source display

If GitHub reports non-mergeable status, update this branch from `main` and rerun validation before merging.
