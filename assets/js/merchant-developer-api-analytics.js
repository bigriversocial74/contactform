document.addEventListener('DOMContentLoaded',function(){
'use strict';
if(!window.Microgifter||!document.querySelector('[data-dev-api-analytics-kpis]'))return;
var devData=null;
function esc(v){var s=String(v==null?'':v);return s.split('&').join('&amp;').split('<').join('&lt;').split('>').join('&gt;').split('"').join('&quot;');}
function n(v){return Number(v||0).toLocaleString();}
function pct(used,limit){used=Number(used||0);limit=Number(limit||0);return limit?Math.min(100,Math.round(used*100/limit)):0;}
function row(left,right){return '<div class="mg-health-row"><span>'+left+'</span><span>'+right+'</span></div>';}
function kpi(label,value){return '<div class="mg-merchant-kpi"><span>'+esc(label)+'</span><strong>'+esc(value)+'</strong></div>';}
function renderRows(node,items,fn,empty){if(!node)return;node.innerHTML=(items&&items.length?items.map(fn).join(''):'<div class="mg-empty-state">'+esc(empty||'No data yet.')+'</div>');}
function normalizeDocLinks(){var map={'/docs/stage-api-8-sandbox-flow.md':'/developer-docs.php#quickstart','/docs/stage-api-5-developer-webhooks.md':'#developer-webhooks','/docs/stage-api-12-webhook-secrets-hardening.md':'#developer-webhooks'};document.querySelectorAll('a[href$=".md"]').forEach(function(a){var href=a.getAttribute('href')||'';if(map[href])a.setAttribute('href',map[href]);});}
function setStatus(node,message,type){if(window.Microgifter&&typeof Microgifter.setStatus==='function'){Microgifter.setStatus(node,message,type);return;}if(node)node.textContent=message||'';}
function goStatus(message,type){var node=document.querySelector('[data-dev-api-launch-status]');if(!node)return;node.textContent=message||'';node.dataset.statusType=type||'';}
function showReveal(data,message){var node=document.querySelector('[data-dev-credential-secret]');if(!node)return;var parts=[];if(data.credential)parts.push('<div class="mg-empty-state"><strong>'+esc(message||'Copy once')+'</strong><p><code>'+esc(data.credential)+'</code></p></div>');if(data.webhook_secret)parts.push('<div class="mg-empty-state"><strong>Copy webhook signing value now</strong><p><code>'+esc(data.webhook_secret)+'</code></p><p>Hint: '+esc(data.webhook_secret_hint||'')+'</p></div>');node.innerHTML=parts.join('');}
function decodeJsonList(value){try{var out=JSON.parse(value||'[]');return Array.isArray(out)?out:[];}catch(e){return [];}}
function appId(app){return app.public_id||app.app_id||'';}
function renderSummaryKpis(data){
  var node=document.querySelector('[data-dev-api-kpis]');
  if(!node)return;
  var summary=data.summary||{},setup=data.onboarding||{},completed=Number(setup.completed||0),total=Number(setup.total||(setup.steps||[]).length||0),progress=total?Math.round(completed*100/total):0;
  node.innerHTML=[
    kpi('Apps',n(summary.app_count)),
    kpi('Active apps',n(summary.active_apps)),
    kpi('Credentials',n(summary.key_count)),
    kpi('Active credentials',n(summary.active_keys)),
    kpi('Setup progress',progress+'%')
  ].join('');
}
function renderOnboarding(data){
  var setup=data.onboarding||{},node=document.querySelector('[data-dev-api-onboarding]'),badge=document.querySelector('[data-dev-api-readiness]');
  if(!node)return;
  var steps=setup.steps||[],completed=Number(setup.completed||0),total=Number(setup.total||steps.length||0),progress=total?Math.round(completed*100/total):0;
  if(badge){badge.textContent=setup.ready_for_live?'Live ready':(setup.ready_for_test?'Test ready':'Setup needed');}
  node.innerHTML='<div class="mg-program-progress" style="margin-bottom:14px"><span style="width:'+progress+'%"></span></div>'+steps.map(function(s){var done=!!s.done,href=s.action_href||'#',label=s.action_label||'Open';return '<div class="mg-health-row"><span><strong>'+(done?'✓ ':'○ ')+esc(s.label)+'</strong><br><small>'+esc(s.detail)+'</small></span><a href="'+esc(href)+'">'+esc(label)+'</a></div>';}).join('')+'<p style="margin-top:12px;color:#64748b;font-size:13px">'+completed+' of '+total+' setup steps complete.</p>';
}
function stateIcon(state){return state==='ok'?'✓':(state==='warn'?'!':'×');}
function appActions(app){var out=[];if(app.environment==='test')out.push('<button type="button" class="mg-btn mg-btn-soft" data-go-live-action="clone_to_live" data-app-id="'+esc(app.app_id)+'">Create live app</button>');if(app.environment==='live'&&app.status!=='active')out.push('<button type="button" class="mg-btn mg-btn-primary" data-go-live-action="promote_live" data-app-id="'+esc(app.app_id)+'">Promote live</button>');if(app.environment==='live')out.push('<button type="button" class="mg-btn mg-btn-soft" data-go-live-action="create_live_credential" data-app-id="'+esc(app.app_id)+'">Create live credential</button>');return out.length?'<div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:12px">'+out.join('')+'</div>':'';}
function wireGoLiveActions(){document.querySelectorAll('[data-go-live-action]').forEach(function(btn){btn.onclick=async function(){var action=btn.dataset.goLiveAction,appId=btn.dataset.appId,payload={action:action,app_id:appId};if(action==='create_live_credential')payload.name='Live credential';try{btn.disabled=true;goStatus('Working…');var response=await Microgifter.post('/api/merchant/developer-api-go-live.php',payload),data=response.data||response;showReveal(data,response.message);goStatus(response.message||'Go-live action completed.','success');await loadDeveloperAnalytics();await loadLaunchQA();}catch(err){goStatus(err.message||'Unable to complete go-live action.','error');}finally{btn.disabled=false;}};});}
function renderLaunchQA(data){
  var node=document.querySelector('[data-dev-api-launch-qa]'),badge=document.querySelector('[data-dev-api-launch-status]');
  if(!node)return;
  var summary=data.summary||{},apps=data.apps||[];
  if(badge){badge.textContent=summary.ready_for_launch?'Launch ready':'Needs QA';}
  var head='<div class="mg-merchant-kpis" style="margin-bottom:14px">'+[kpi('Ready apps',n(summary.ready_apps)),kpi('Blocked apps',n(summary.blocked_apps)),kpi('Live apps',n(summary.live_apps)),kpi('Warnings',n(summary.warning_apps))].join('')+'</div>';
  if(!apps.length){node.innerHTML=head+'<div class="mg-empty-state">No developer apps yet.</div>';return;}
  node.innerHTML=head+apps.map(function(app){var checks=app.checks||[];return '<div class="mg-health-row" style="align-items:flex-start"><span><strong>'+esc(app.name)+'</strong><br><small>'+esc(app.environment)+' · '+esc(app.status)+' · '+n(app.blockers)+' blockers · '+n(app.warnings)+' warnings</small><div style="margin-top:10px">'+checks.map(function(c){return '<div><small><strong>'+stateIcon(c.state)+' '+esc(c.label)+':</strong> '+esc(c.message)+'</small></div>';}).join('')+'</div>'+appActions(app)+'</span><span>'+esc(app.ready?'Ready':'Review')+'</span></div>';}).join('');
  wireGoLiveActions();
}
async function loadLaunchQA(){var node=document.querySelector('[data-dev-api-launch-qa]');if(!node)return;try{var response=await Microgifter.get('/api/merchant/developer-api-launch-qa.php');renderLaunchQA(response.data||response);}catch(err){node.innerHTML='<div class="mg-empty-state">Unable to load live launch QA.</div>';}}
function fillProgramSelect(select,programs,selected){if(!select)return;select.innerHTML='<option value="">No default program</option>'+(programs||[]).map(function(p){var id=p.public_id||'';return '<option value="'+esc(id)+'" '+(id===selected?'selected':'')+'>'+esc(p.name||id)+' ('+esc(p.status||'')+')</option>';}).join('');}
function fillAppSelect(select,apps,selected){if(!select)return;select.innerHTML='<option value="">Select app</option>'+(apps||[]).map(function(app){var id=appId(app);return '<option value="'+esc(id)+'" '+(id===selected?'selected':'')+'>'+esc(app.name||id)+' ('+esc(app.environment||'test')+')</option>';}).join('');}
function editApp(app){var form=document.querySelector('[data-dev-api-form]');if(!form)return;form.elements.app_id.value=appId(app);form.elements.name.value=app.name||'';form.elements.environment.value=app.environment||'test';form.elements.status.value=app.status||'active';form.elements.default_program_id.value=app.default_program_id||'';form.elements.allowed_origins.value=decodeJsonList(app.allowed_origins_json).join('\n');form.elements.webhook_url.value=app.webhook_url||'';setStatus(form.querySelector('[data-dev-api-status]'),'Editing '+(app.name||'developer app')+'.');}
function resetAppForm(){var form=document.querySelector('[data-dev-api-form]');if(!form)return;form.reset();form.elements.app_id.value='';form.elements.environment.value='test';form.elements.status.value='active';setStatus(form.querySelector('[data-dev-api-status]'),'');}
function renderDeveloperApps(data){var apps=data.apps||[],programs=data.programs||[];fillProgramSelect(document.querySelector('[data-dev-api-programs]'),programs,'');fillAppSelect(document.querySelector('[data-dev-credential-apps]'),apps,'');renderRows(document.querySelector('[data-dev-api-apps]'),apps,function(app){var id=appId(app);return row('<strong>'+esc(app.name)+'</strong><br><small>'+esc(app.environment)+' · '+esc(app.status)+' · '+esc(app.default_program_name||'No default program')+'</small><br><small>Webhook: '+esc(app.webhook_url||'not configured')+' · Secret: '+(app.webhook_secret_configured?'configured':'missing')+'</small>','<button type="button" class="mg-btn mg-btn-soft" data-dev-edit-app="'+esc(id)+'">Edit</button>');},'No developer apps yet. Create the first app with the editor.');document.querySelectorAll('[data-dev-edit-app]').forEach(function(btn){btn.onclick=function(){var app=apps.find(function(x){return appId(x)===btn.dataset.devEditApp;});if(app)editApp(app);};});}
function renderKeysAndLogs(data){renderRows(document.querySelector('[data-dev-api-keys]'),data.keys||[],function(k){return row('<strong>'+esc(k.name)+'</strong><br><small>'+esc(k.app_name)+' · '+esc(k.key_prefix)+'… · '+esc(k.environment)+' · '+esc(k.status)+'</small>','<button type="button" class="mg-btn mg-btn-soft" data-dev-revoke-key="'+esc(k.public_id)+'" '+(k.status==='revoked'?'disabled':'')+'>Revoke</button>');},'No credentials yet.');document.querySelectorAll('[data-dev-revoke-key]').forEach(function(btn){btn.onclick=async function(){if(!confirm('Revoke this credential?'))return;try{btn.disabled=true;var r=await Microgifter.post('/api/merchant/developer-api-credentials.php',{action:'revoke',credential_id:btn.dataset.devRevokeKey});showReveal({},r.message||'Credential revoked.');await loadDeveloperAnalytics();}catch(err){showReveal({},err.message||'Unable to revoke credential.');}finally{btn.disabled=false;}};});renderRows(document.querySelector('[data-dev-api-logs]'),data.logs||[],function(x){return row('<strong>'+esc(x.method)+' '+esc(x.endpoint)+'</strong><br><small>'+esc(x.app_name||'')+' · '+esc(x.key_prefix||'')+' · '+esc(x.created_at||'')+'</small><br><small>'+esc(x.error_message||x.response_status||'')+'</small>','<strong>'+esc(x.status_code)+'</strong>');},'No public API requests logged yet.');}
function wireDeveloperForms(){var appForm=document.querySelector('[data-dev-api-form]');if(appForm&&!appForm.dataset.wired){appForm.dataset.wired='1';appForm.addEventListener('submit',async function(e){e.preventDefault();var status=appForm.querySelector('[data-dev-api-status]'),btn=appForm.querySelector('[type="submit"]');var payload=Object.fromEntries(new FormData(appForm).entries());payload.action='save_app';payload.allowed_origins=String(payload.allowed_origins||'').split(/[\n,]+/).map(function(x){return x.trim();}).filter(Boolean);try{btn.disabled=true;setStatus(status,'Saving developer app…');var r=await Microgifter.post('/api/merchant/developer-api.php',payload);setStatus(status,r.message||'Developer app saved.','success');showReveal(r.data||r,r.message);resetAppForm();await loadDeveloperAnalytics();await loadLaunchQA();}catch(err){setStatus(status,err.message||'Unable to save developer app.','error');}finally{btn.disabled=false;}});}var credForm=document.querySelector('[data-dev-credential-form]');if(credForm&&!credForm.dataset.wired){credForm.dataset.wired='1';credForm.addEventListener('submit',async function(e){e.preventDefault();var status=credForm.querySelector('[data-dev-credential-status]'),btn=credForm.querySelector('[type="submit"]');var payload=Object.fromEntries(new FormData(credForm).entries());payload.action='create';try{btn.disabled=true;setStatus(status,'Creating credential…');var r=await Microgifter.post('/api/merchant/developer-api-credentials.php',payload);setStatus(status,r.message||'Credential created.','success');showReveal(r.data||r,r.message);credForm.reset();await loadDeveloperAnalytics();}catch(err){setStatus(status,err.message||'Unable to create credential.','error');}finally{btn.disabled=false;}});}}
async function loadDeveloperAnalytics(){
  try{
    var response=await Microgifter.get('/api/merchant/developer-api.php');
    var data=response.data||response;devData=data;
    renderSummaryKpis(data);renderOnboarding(data);renderDeveloperApps(data);renderKeysAndLogs(data);wireDeveloperForms();normalizeDocLinks();
    var analytics=data.analytics||{},totals=analytics.totals||{},sandbox=analytics.sandbox||{},kpis=document.querySelector('[data-dev-api-analytics-kpis]');
    if(kpis){kpis.innerHTML=[kpi('Total requests',n(totals.total_requests)),kpi('Requests 24h',n(totals.requests_24h)),kpi('Errors 24h',n(totals.errors_24h)),kpi('Rate limits 24h',n(totals.rate_limited_24h)),kpi('Sandbox rewards',n(sandbox.sandbox_rewards)),kpi('Sandbox 24h',n(sandbox.sandbox_rewards_24h))].join('');}
    renderRows(document.querySelector('[data-dev-api-daily]'),analytics.daily,function(x){return row('<strong>'+esc(x.day)+'</strong><br><small>'+n(x.errors)+' errors · '+n(x.rate_limited)+' limited</small>','<strong>'+n(x.requests)+'</strong>');},'No request history yet.');
    renderRows(document.querySelector('[data-dev-api-app-usage]'),analytics.apps,function(x){return row('<strong>'+esc(x.name)+'</strong><br><small>'+esc(x.environment)+' · '+esc(x.status)+' · '+esc(x.last_request_at||'No requests')+'</small>','<strong>'+n(x.requests_7d)+'</strong><br><small>'+n(x.errors_7d)+' errors</small>');},'No app request usage yet.');
    renderRows(document.querySelector('[data-dev-api-key-usage]'),analytics.keys,function(x){return row('<strong>'+esc(x.name)+'</strong><br><small>'+esc(x.app_name)+' · '+esc(x.key_prefix)+'… · '+esc(x.environment)+'</small>','<strong>'+n(x.requests_7d)+'</strong><br><small>'+n(x.rate_limited_7d)+' limited</small>');},'No credential request usage yet.');
    renderRows(document.querySelector('[data-dev-api-quota-buckets]'),analytics.quota_buckets,function(x){var progress=pct(x.used_count,x.limit_value);return '<div class="mg-health-row"><span><strong>'+esc(x.app_name)+'</strong><br><small>'+esc(x.bucket_scope)+' · '+esc(x.key_prefix)+'… · resets '+esc(x.window_end)+'</small><div class="mg-program-progress"><span style="width:'+progress+'%"></span></div></span><span>'+n(x.used_count)+' / '+n(x.limit_value)+'</span></div>';},'No active quota buckets yet.');
    renderRows(document.querySelector('[data-dev-api-webhook-analytics]'),analytics.webhooks,function(x){return row('<strong>'+esc(x.event_type)+'</strong><br><small>'+esc(x.status)+' · '+esc(x.last_event_at||'No recent events')+'</small>','<strong>'+n(x.events)+'</strong>');},'No webhook events in the last seven days.');
  }catch(err){var node=document.querySelector('[data-dev-api-analytics-kpis]');if(node)node.innerHTML='<div class="mg-empty-state">Unable to load developer API analytics.</div>';}
}
normalizeDocLinks();
loadDeveloperAnalytics();
loadLaunchQA();
});
