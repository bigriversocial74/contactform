(function(){
'use strict';
var root=document.querySelector('[data-watch-video-reward]');if(!root||!window.Microgifter){return;}
var form=root.querySelector('[data-watch-reward-form]');var status=root.querySelector('[data-watch-reward-status]');var result=root.querySelector('[data-watch-reward-result]');var shell=root.querySelector('[data-watch-video-shell]');var provider=root.getAttribute('data-video-provider')||'youtube';var videoId=root.getAttribute('data-video-id')||'';var uploadedUrl=root.getAttribute('data-uploaded-video-url')||'';var uploadedAssetId=root.getAttribute('data-uploaded-asset-id')||'';var campaignId=root.getAttribute('data-campaign-id')||'';var customer={};var player=null;var uploadedPlayer=root.querySelector('[data-watch-uploaded-player]');var timer=null;var maxPercent=0;var lastPost=0;var started=false;
function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'})[c];});}
function setStatus(msg){if(status)status.textContent=msg||'';}
function setResult(html){if(result){result.innerHTML=html||'';result.classList.toggle('is-visible',!!html);}}
function updateMilestones(issued){issued=issued||[];issued.forEach(function(item){var p=String(item.percent||'').replace(/[^0-9]/g,'');var row=root.querySelector('[data-watch-milestone="'+p+'"]');if(row){row.classList.remove('is-warn');row.classList.add('is-ready');}});}
function inboxResult(issued){var details=issued.map(function(i){return esc((i.percent||'')+'% — '+(i.reward_title||'Video reward'));}).join('<br>');return '<strong>Reward sent to your Microgifter Inbox</strong><p>'+details+'</p><p class="mg-public-campaign-note">Open your Microgifter Inbox to view, manage, or redeem the reward.</p><a class="mg-btn mg-btn-primary" href="/inbox.php">Open Microgifter Inbox</a>';}
function currentProgress(){if(provider==='uploaded'&&uploadedPlayer){var duration=Number(uploadedPlayer.duration||0);var current=Number(uploadedPlayer.currentTime||0);return {duration:duration,current:current,percent:duration>0?Math.max(maxPercent,(current/duration)*100):maxPercent};}if(player&&player.getDuration&&player.getCurrentTime){var d=Number(player.getDuration()||0);var c=Number(player.getCurrentTime()||0);return {duration:d,current:c,percent:d>0?Math.max(maxPercent,(c/d)*100):maxPercent};}return {duration:0,current:0,percent:maxPercent};}
function payload(percent){var progress=currentProgress();return {campaign_id:campaignId,email:customer.email||'',name:customer.name||'',phone:customer.phone||'',video_provider:provider,video_id:videoId,uploaded_asset_id:uploadedAssetId,uploaded_video_url:uploadedUrl,progress_percent:Math.max(0,Math.min(100,Math.round(percent))),duration_seconds:Math.round(progress.duration||0),current_time_seconds:Math.round(progress.current||0)};}
async function postProgress(percent,force){var now=Date.now();if(!force&&now-lastPost<4500)return;lastPost=now;try{var response=await Microgifter.post('/api/public/campaigns/watch-progress.php',payload(percent));var data=response.data||response;var issued=data.issued_rewards||[];updateMilestones(issued);if(issued.length){setResult(inboxResult(issued));}setStatus(data.message||('Watch progress recorded: '+Math.round(percent)+'%'));}catch(error){setStatus(error.message||'Unable to record watch progress.');}}
function tick(){var progress=currentProgress();if(progress.duration>0){maxPercent=Math.min(100,progress.percent);setStatus('Watching… '+Math.round(maxPercent)+'% complete');postProgress(maxPercent,false);}}
function onPlayerStateChange(event){if(event.data===YT.PlayerState.PLAYING){if(!started){started=true;postProgress(1,true);}clearInterval(timer);timer=setInterval(tick,3000);}else if(event.data===YT.PlayerState.PAUSED){tick();clearInterval(timer);}else if(event.data===YT.PlayerState.ENDED){maxPercent=100;tick();postProgress(100,true);clearInterval(timer);setStatus('Video complete. Final rewards checked.');}}
window.onYouTubeIframeAPIReady=function(){if(provider!=='youtube'||!videoId)return;player=new YT.Player('mg-watch-video-player',{videoId:videoId,playerVars:{rel:0,modestbranding:1,playsinline:1,origin:location.origin},events:{onStateChange:onPlayerStateChange,onReady:function(){setStatus('Video ready. Press play to start earning rewards.');}}});};
function bindUploadedPlayer(){if(provider!=='uploaded'||!uploadedPlayer)return;uploadedPlayer.addEventListener('play',function(){if(!started){started=true;postProgress(1,true);}clearInterval(timer);timer=setInterval(tick,3000);});uploadedPlayer.addEventListener('pause',function(){tick();clearInterval(timer);});uploadedPlayer.addEventListener('timeupdate',tick);uploadedPlayer.addEventListener('ended',function(){maxPercent=100;postProgress(100,true);clearInterval(timer);setStatus('Video complete. Final rewards checked.');});setStatus('Uploaded video ready. Press play to start earning rewards.');}
function readForm(){var fd=new FormData(form);return {name:String(fd.get('name')||'').trim(),email:String(fd.get('email')||'').trim().toLowerCase(),phone:String(fd.get('phone')||'').trim()};}
if(form){form.addEventListener('submit',function(event){event.preventDefault();customer=readForm();if(!customer.email||customer.email.indexOf('@')<1){setStatus('Enter a valid email before watching.');return;}form.hidden=true;if(shell)shell.hidden=false;if(provider==='uploaded'){bindUploadedPlayer();postProgress(0,true);return;}setStatus('Loading YouTube player…');if(window.YT&&window.YT.Player){window.onYouTubeIframeAPIReady();}postProgress(0,true);});}
})();
