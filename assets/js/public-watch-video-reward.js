(function(){'use strict';var root=document.querySelector('[data-watch-video-reward]');if(!root)return;var form=root.querySelector('[data-watch-reward-form]'),result=root.querySelector('[data-watch-reward-result]'),shell=root.querySelector('[data-watch-video-shell]'),provider=root.getAttribute('data-video-provider')||'youtube',videoId=root.getAttribute('data-video-id')||'',uploadedUrl=root.getAttribute('data-uploaded-video-url')||'',uploadedAssetId=root.getAttribute('data-uploaded-asset-id')||'',campaignId=root.getAttribute('data-campaign-id')||'',statusNodes=[].slice.call(root.querySelectorAll('[data-watch-reward-status]')),notifications=root.querySelector('[data-watch-reward-notifications]'),history=root.querySelector('[data-watch-reward-history]'),rewardHistory=root.querySelector('[data-watch-reward-issue-history]'),uploadedPlayer=root.querySelector('[data-watch-uploaded-player]'),customer={},player=null,timer=null,maxPercent=0,lastPost=0,unlockedStep=0,started=false,uploadBound=false,steps={join:0,media:1,rewards:2};function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'})[c];});}function time(){try{return new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'});}catch(e){return'';}}function listAdd(list,msg){if(!list||!msg)return;if(list.children.length===1&&/waiting|no watch|no rewards/i.test(list.children[0].textContent||''))list.innerHTML='';var li=document.createElement('li');li.innerHTML='<span>'+esc(time())+'</span><strong>'+esc(msg)+'</strong>';list.insertBefore(li,list.firstChild||null);while(list.children.length>8)list.removeChild(list.lastChild);}function setStatus(msg){statusNodes.forEach(function(n){n.textContent=msg||'';});listAdd(notifications,msg);}function setResult(html){if(!result)return;result.innerHTML=html||'';result.classList.toggle('is-visible',!!html);}function syncTabs(){root.querySelectorAll('[data-watch-tab-trigger]').forEach(function(b){var n=b.getAttribute('data-watch-tab-trigger')||'join',i=steps[n]||0;b.setAttribute('aria-disabled',i>unlockedStep?'true':'false');b.classList.toggle('is-complete',i<unlockedStep);});}function showTab(n,force){if(steps[n]==null)n='join';if(!force&&(steps[n]||0)>unlockedStep){setStatus('Complete the current step before moving forward.');return;}root.querySelectorAll('[data-watch-tab-trigger]').forEach(function(b){var a=b.getAttribute('data-watch-tab-trigger')===n;b.classList.toggle('is-active',a);b.setAttribute('aria-selected',a?'true':'false');});root.querySelectorAll('[data-watch-tab-panel]').forEach(function(p){var a=p.getAttribute('data-watch-tab-panel')===n;p.classList.toggle('is-active',a);p.hidden=!a;});syncTabs();}function unlock(i){unlockedStep=Math.max(unlockedStep,i);syncTabs();}root.querySelectorAll('[data-watch-tab-trigger]').forEach(function(b){b.addEventListener('click',function(){showTab(b.getAttribute('data-watch-tab-trigger')||'join',false);});});syncTabs();function card(item){var img=item.reward_image_url?'<img class="mg-campaign-issued-reward-image" src="'+esc(item.reward_image_url)+'" alt="">':'';return'<div class="mg-campaign-issued-reward-card '+(img?'has-image':'is-text-only')+'">'+img+'<span>'+esc((item.percent||'')+'% — '+(item.reward_title||'Video reward'))+'</span></div>';}function inbox(issued){return'<strong>Reward sent to your Microgifter Inbox</strong><div class="mg-campaign-issued-reward-list">'+issued.map(card).join('')+'</div><p class="mg-public-campaign-note">Open your Microgifter Inbox to view, manage, or redeem the reward.</p><a class="mg-btn mg-btn-primary" href="/inbox.php">Open Microgifter Inbox</a>';}function notice(msg){var account=/account|required|signed-in|sign in|signed in/i.test(msg||''),actions=account?'<a class="mg-btn mg-btn-primary" href="/signin.php">Sign in</a><a class="mg-btn mg-btn-soft" href="/signup.php">Create account</a>':'<a class="mg-btn mg-btn-primary" href="/inbox.php">Open Microgifter Inbox</a>';unlock(2);setResult('<strong>Campaign notice</strong><p>'+esc(msg||'This campaign is not available for another participation.')+'</p><div class="mg-heading-actions">'+actions+'</div>');showTab('rewards',true);}function readForm(){var fd=new FormData(form);return{name:String(fd.get('name')||'').trim(),email:String(fd.get('email')||'').trim().toLowerCase(),phone:String(fd.get('phone')||'').trim()};}function progress(){if(provider==='uploaded'&&uploadedPlayer){var d=Number(uploadedPlayer.duration||0),c=Number(uploadedPlayer.currentTime||0);return{duration:d,current:c,percent:d>0?Math.max(maxPercent,(c/d)*100):maxPercent};}if(player&&player.getDuration&&player.getCurrentTime){var yd=Number(player.getDuration()||0),yc=Number(player.getCurrentTime()||0);return{duration:yd,current:yc,percent:yd>0?Math.max(maxPercent,(yc/yd)*100):maxPercent};}return{duration:0,current:0,percent:maxPercent};}function payload(percent){var p=progress();return{campaign_id:campaignId,email:customer.email||'',name:customer.name||'',phone:customer.phone||'',video_provider:provider,video_id:videoId,uploaded_asset_id:uploadedAssetId,uploaded_video_url:uploadedUrl,progress_percent:Math.max(0,Math.min(100,Math.round(percent))),duration_seconds:Math.round(p.duration||0),current_time_seconds:Math.round(p.current||0)};}async function post(percent,force){if(!window.Microgifter||typeof Microgifter.post!=='function'){setStatus('Microgifter reward tracking is still loading.');return;}var now=Date.now();if(!force&&now-lastPost<4500)return;lastPost=now;try{var r=await Microgifter.post('/api/public/campaigns/watch-progress-v2.php',payload(percent)),d=r.data||r,issued=d.issued_rewards||[];issued.forEach(function(i){listAdd(rewardHistory,(i.percent||'')+'% reward issued — '+(i.reward_title||'Video reward'));});if(issued.length){unlock(2);setResult(inbox(issued));showTab('rewards',true);}setStatus(d.message||('Watch progress recorded: '+Math.round(percent)+'%'));listAdd(history,'Progress recorded at '+Math.round(percent)+'%.');}catch(e){var m=e.message||'Unable to record watch progress.';setStatus(m);if(/already participated|limit reached|account required|required to participate|signed-in|signed in|sign in/i.test(m))notice(m);}}function tick(){var p=progress();if(p.duration>0){maxPercent=Math.min(100,p.percent);setStatus('Watching… '+Math.round(maxPercent)+'% complete');post(maxPercent,false);}}function state(e){if(e.data===YT.PlayerState.PLAYING){if(!started){started=true;listAdd(history,'Watch session started.');post(1,true);}clearInterval(timer);timer=setInterval(tick,3000);}else if(e.data===YT.PlayerState.PAUSED){tick();clearInterval(timer);}else if(e.data===YT.PlayerState.ENDED){maxPercent=100;post(100,true);clearInterval(timer);setStatus('Video complete. Final rewards checked.');listAdd(history,'Video completed.');}}window.onYouTubeIframeAPIReady=function(){if(provider!=='youtube'||!videoId)return;player=new YT.Player('mg-watch-video-player',{videoId:videoId,playerVars:{rel:0,modestbranding:1,playsinline:1,origin:location.origin},events:{onStateChange:state,onReady:function(){setStatus('Video ready. Press play to start earning rewards.');}}});};function bindUpload(){if(provider!=='uploaded'||!uploadedPlayer||uploadBound)return;uploadBound=true;uploadedPlayer.addEventListener('play',function(){if(!started){started=true;listAdd(history,'Watch session started.');post(1,true);}clearInterval(timer);timer=setInterval(tick,3000);});uploadedPlayer.addEventListener('pause',function(){tick();clearInterval(timer);});uploadedPlayer.addEventListener('timeupdate',tick);uploadedPlayer.addEventListener('ended',function(){maxPercent=100;post(100,true);clearInterval(timer);setStatus('Video complete. Final rewards checked.');listAdd(history,'Video completed.');});setStatus('Uploaded video ready. Press play to start earning rewards.');}if(form)form.addEventListener('submit',function(e){e.preventDefault();customer=readForm();if(!customer.email||customer.email.indexOf('@')<1){setStatus('Enter a valid email before watching.');return;}if(shell)shell.hidden=false;unlock(1);showTab('media',true);listAdd(history,'Campaign joined for '+customer.email+'.');if(provider==='uploaded'){bindUpload();post(0,true);return;}setStatus('Loading YouTube player…');if(window.YT&&window.YT.Player)window.onYouTubeIframeAPIReady();post(0,true);});})();
