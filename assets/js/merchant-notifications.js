document.addEventListener('DOMContentLoaded',function(){
'use strict';
if(!window.Microgifter)return;
var root=document.querySelector('[data-merchant-app][data-merchant-view="notifications"]');
if(!root)return;
var feed=root.querySelector('[data-merchant-notification-feed]');
var tabs=root.querySelector('[data-merchant-notification-tabs]');
var status=root.querySelector('[data-merchant-notification-status]');
var kpis=root.querySelector('[data-merchant-notification-kpis]');
var panel=root.querySelector('[data-merchant-customer-thread-panel]');
var messages=root.querySelector('[data-merchant-thread-messages]');
var replyForm=root.querySelector('[data-merchant-thread-reply-form]');
var threadStatus=root.querySelector('[data-merchant-thread-status]');
if(!feed||!tabs)return;
function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'})[c];});}
function title(v){return String(v||'').replace(/_/g,' ').replace(/\b\w/g,function(c){return c.toUpperCase();});}
function setStatus(message){if(status)status.textContent=message||'';}
function setThreadStatus(message){if(threadStatus)threadStatus.textContent=message||'';}
function time(v){var t=Date.parse(v||'');return t?new Date(t).toLocaleString():'';}
function icon(kind){return({tips:'$',messages:'✉',rewards:'★',redemptions:'✓',all:'•'})[kind]||'•';}
function label(item){if(item.kind==='messages')return'Open message';if(item.kind==='tips')return'View tip';if(item.kind==='redemptions')return'View claim';if(item.kind==='rewards')return'View reward';return'Open';}
function uuid(v){return /^[0-9a-f-]{36}$/i.test(String(v||''));}
function profileUrl(c){c=c||{};if(uuid(c.wallet_item_id))return'/merchant-customer.php?wallet_item_id='+encodeURIComponent(c.wallet_item_id);if(uuid(c.contact_public_id))return'/merchant-customer.php?campaign_contact_id='+encodeURIComponent(c.contact_public_id);if(uuid(c.contact_id))return'/merchant-customer.php?campaign_contact_id='+encodeURIComponent(c.contact_id);if(c.customer_user_id)return'/merchant-customer.php?user_id='+encodeURIComponent(c.customer_user_id);return'';}
function card(item){var c=item.context||{};var meta=[item.source==='alert'?'Operational alert':'User notification',title(item.kind),time(item.created_at)].filter(Boolean).join(' · ');var tags=[item.severity,item.status].filter(Boolean).map(function(x){return'<em>'+esc(title(x))+'</em>';}).join('');var context=[c.wallet_item_id?'Wallet '+c.wallet_item_id:'',c.campaign_id?'Campaign '+c.campaign_id:'',c.tip_id?'Tip '+c.tip_id:'',c.message_id?'Message '+c.message_id:''].filter(Boolean).map(esc).join(' · ');var customerLink=profileUrl(c);return'<article class="mg-merchant-notification-card '+(item.is_unread?'is-unread':'')+'" data-source="'+esc(item.source)+'" data-id="'+esc(item.id)+'"><div class="mg-merchant-notification-icon">'+esc(icon(item.kind))+'</div><div class="mg-merchant-notification-content"><div class="mg-merchant-notification-top"><span>'+esc(meta)+'</span><span class="mg-card-meta">'+tags+'</span></div><h3>'+esc(item.title||'Merchant notification')+'</h3><p>'+esc(item.body||'No message body was provided.')+'</p><div class="mg-merchant-notification-context">'+context+'</div><div class="mg-merchant-notification-actions"><a class="mg-btn mg-btn-secondary" href="'+esc(item.action_url||'/merchant-notifications.php')+'">'+esc(label(item))+'</a>'+(customerLink?'<a class="mg-btn mg-btn-secondary" href="'+esc(customerLink)+'" data-view-customer>View customer</a>':'')+(item.is_unread?'<button class="mg-btn mg-btn-primary" type="button" data-notification-done>Acknowledge</button>':'')+'</div></div></article>';}
function messageRow(m){return'<div class="mg-merchant-thread-message '+(m.mine?'is-mine':'')+'"><span>'+esc(m.sender_name||'User')+' · '+esc(time(m.created_at))+'</span><p>'+esc(m.body||'')+'</p></div>';}
var params=new URLSearchParams(window.location.search||'');
var current=params.get('filter')||'all';
var openItem=params.get('item')||params.get('wallet_item_id')||'';
async function loadThread(item){if(!item||!panel||!messages)return;panel.hidden=false;setThreadStatus('Loading thread…');try{var r=await Microgifter.get('/api/merchant/customer-thread.php?item='+encodeURIComponent(item));var data=r.data||r;var thread=data.thread||{};var titleNode=root.querySelector('[data-merchant-thread-title]');var metaNode=root.querySelector('[data-merchant-thread-meta]');if(titleNode)titleNode.textContent=thread.subject||'Customer thread';if(metaNode){var threadProfile=profileUrl({wallet_item_id:thread.wallet_item_id,contact_public_id:thread.contact_id,customer_user_id:thread.customer_user_id});var meta=[thread.customer_name,thread.customer_email,thread.campaign_title,thread.status].filter(Boolean).map(esc).join(' · ');metaNode.innerHTML=meta+(threadProfile?' · <a href="'+esc(threadProfile)+'">View customer profile</a>':'');}messages.innerHTML=(thread.messages||[]).length?(thread.messages||[]).map(messageRow).join(''):'<div class="mg-empty-state"><p>No thread messages yet.</p></div>';if(replyForm&&replyForm.elements.thread_id)replyForm.elements.thread_id.value=thread.id||('wallet:'+item);setThreadStatus((thread.messages||[]).length+' message'+((thread.messages||[]).length===1?'':'s'));panel.scrollIntoView({behavior:'smooth',block:'start'});}catch(e){messages.innerHTML='<div class="mg-empty-state"><strong>Unable to load customer thread</strong><p>'+esc(e.message||'Try again in a moment.')+'</p></div>';setThreadStatus('Thread load failed');}}
async function refresh(){setStatus('Loading '+title(current)+'…');try{var r=await Microgifter.get('/api/merchant/notifications.php?filter='+encodeURIComponent(current)+'&limit=50');var data=r.data||r;var counts=data.counts||{};tabs.querySelectorAll('[data-filter]').forEach(function(btn){var f=btn.dataset.filter;btn.classList.toggle('is-active',f===current);var n=btn.querySelector('[data-count]');if(n)n.textContent=Number(counts[f]||0).toLocaleString();});if(kpis){kpis.innerHTML=[['Unread',counts.unread||0],['Tips',counts.tips||0],['Messages',counts.messages||0],['Rewards',counts.rewards||0]].map(function(v){return'<div class="mg-merchant-kpi"><span>'+esc(v[0])+'</span><strong>'+Number(v[1]).toLocaleString()+'</strong></div>';}).join('');}var items=data.items||[];feed.innerHTML=items.length?items.map(card).join(''):'<div class="mg-empty-state"><strong>No merchant notifications</strong><p>This queue will show tips, voucher messages, reward movement, and redemption alerts as customers interact with wallet rewards.</p></div>';setStatus(Number(items.length).toLocaleString()+' item'+(items.length===1?'':'s'));}catch(e){feed.innerHTML='<div class="mg-empty-state"><strong>Unable to load notifications</strong><p>'+esc(e.message||'Try again in a moment.')+'</p></div>';setStatus('Load failed');}}
tabs.addEventListener('click',function(e){var btn=e.target.closest('[data-filter]');if(!btn)return;current=btn.dataset.filter||'all';refresh();});
var refreshButton=root.querySelector('[data-merchant-notification-refresh]');if(refreshButton)refreshButton.addEventListener('click',refresh);
feed.addEventListener('click',async function(e){var btn=e.target.closest('[data-notification-done]');if(!btn)return;var row=btn.closest('[data-source][data-id]');if(!row)return;try{btn.disabled=true;btn.textContent='Saving…';await Microgifter.post('/api/merchant/notifications.php',{source:row.dataset.source,id:row.dataset.id,action:'acknowledge'});await refresh();}catch(err){btn.disabled=false;btn.textContent='Acknowledge';setStatus(err.message||'Unable to acknowledge.');}});
if(replyForm){replyForm.addEventListener('submit',async function(e){e.preventDefault();var fd=new FormData(replyForm);var body=String(fd.get('body')||'').trim();var thread=String(fd.get('thread_id')||'').trim();if(!body||!thread){setThreadStatus('Thread and reply body are required.');return;}try{setThreadStatus('Sending reply…');await Microgifter.post('/api/merchant/customer-thread.php',{thread_id:thread,body:body,idempotency_key:'merchant-reply-'+Date.now()+'-'+Math.random().toString(16).slice(2)});replyForm.reset();if(replyForm.elements.thread_id)replyForm.elements.thread_id.value=thread;setThreadStatus('Reply sent.');await loadThread(thread.replace(/^wallet:/,''));await refresh();}catch(err){setThreadStatus(err.message||'Unable to send reply.');}});}
refresh().then(function(){if(openItem)loadThread(openItem);});
});
