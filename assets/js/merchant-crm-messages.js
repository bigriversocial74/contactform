document.addEventListener('DOMContentLoaded',function(){
'use strict';
if(!window.Microgifter)return;var host=document.querySelector('[data-merchant-crm-messages]');if(!host)return;
var state={threads:[],current:null};
function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'})[c];});}
function fmt(v){if(!v)return '—';try{return new Date(String(v).replace(' ','T')).toLocaleString();}catch(e){return String(v);}}
function threadCard(t){return '<article class="mg-product-card '+(t.unread?'is-unread':'')+'" data-crm-thread="'+esc(t.id)+'"><button type="button"><span><strong>'+esc(t.contact_name||t.contact_email||t.subject||'CRM conversation')+'</strong><span>'+esc(t.campaign_title||'CRM')+' · '+esc(t.campaign_type||'')+'</span><small>'+esc(t.latest_message||'Open conversation')+' · '+esc(fmt(t.latest_at))+'</small></span><span class="mg-card-meta"><em>'+(t.unread?'unread':'read')+'</em></span></button></article>';}
function messageBubble(m){return '<article class="mg-message-bubble '+(m.mine?'is-mine':'')+'"><strong>'+esc(m.sender_name||'User')+'</strong><p>'+esc(m.body||'')+'</p><small>'+esc(fmt(m.created_at))+'</small></article>';}
function renderThreads(){var list=host.querySelector('[data-crm-message-thread-list]');if(!list)return;list.innerHTML=state.threads.length?state.threads.map(threadCard).join(''):'<div class="mg-empty-state"><strong>No CRM messages yet</strong><p>Merchant/customer conversations started from CRM contacts will appear here.</p></div>';}
async function loadThreads(){var res=await Microgifter.get('/api/merchant/crm-messages.php?limit=100');var data=res.data||res;state.threads=data.threads||[];var badge=host.querySelector('[data-crm-message-unread]');if(badge)badge.textContent=Number(data.unread_count||0).toLocaleString();renderThreads();if(window.Microgifter.setMessageCount)document.dispatchEvent(new CustomEvent('mg:messages:refresh'));return state.threads;}
async function openThread(id){state.current=id;var detail=host.querySelector('[data-crm-message-detail]');if(detail)detail.innerHTML='<div class="mg-empty-state"><strong>Loading conversation…</strong></div>';var res=await Microgifter.get('/api/merchant/crm-messages.php?thread='+encodeURIComponent(id));var t=(res.data||res).thread||{};if(detail)detail.innerHTML='<div class="mg-thread-detail-head"><div><h2>'+esc(t.contact_name||t.contact_email||t.subject||'CRM conversation')+'</h2><p>'+esc(t.campaign_title||'Merchant CRM')+' · '+esc(t.campaign_type||'')+'</p></div></div><div class="mg-message-stream">'+((t.messages||[]).map(messageBubble).join('')||'<div class="mg-empty-state"><strong>No messages yet.</strong></div>')+'</div><form class="mg-message-composer" data-crm-thread-reply><textarea name="body" maxlength="4000" required placeholder="Reply as merchant"></textarea><button class="mg-btn mg-btn-primary" type="submit">Send</button></form>';var stream=detail&&detail.querySelector('.mg-message-stream');if(stream)stream.scrollTop=stream.scrollHeight;await loadThreads();}
async function sendReply(ev){ev.preventDefault();var form=ev.target;if(!state.current)return;var body=form.elements.body.value.trim();if(!body)return;await Microgifter.post('/api/merchant/crm-messages.php',{thread_id:state.current,body:body});form.reset();await openThread(state.current);document.dispatchEvent(new CustomEvent('mg:messages:refresh'));document.dispatchEvent(new CustomEvent('mg:notifications:refresh'));}
host.innerHTML='<div class="mg-app-panel-head"><div><h2>CRM Messages</h2><p>Merchant-owned customer conversations. Customers see their side in normal Messages; merchants manage CRM threads here.</p></div><div class="mg-heading-actions"><span class="mg-status-badge"><strong data-crm-message-unread>0</strong>&nbsp;unread</span><button class="mg-btn mg-btn-soft" type="button" data-crm-messages-refresh>Refresh</button></div></div><div class="mg-app-panel-body"><div class="mg-communications-split"><div class="mg-thread-list" data-crm-message-thread-list></div><section class="mg-thread-detail" data-crm-message-detail><div class="mg-empty-state"><strong>Select a CRM conversation</strong><p>Open a customer thread to reply from the merchant workspace.</p></div></section></div></div>';
host.addEventListener('click',function(e){var row=e.target.closest('[data-crm-thread]');if(row)openThread(row.getAttribute('data-crm-thread'));});host.addEventListener('submit',function(e){var f=e.target.closest('[data-crm-thread-reply]');if(f)sendReply(e);});
document.addEventListener('click',function(e){if(e.target.closest('[data-crm-messages-refresh]'))loadThreads();});
document.addEventListener('mg:crm-messages:refresh',function(e){loadThreads().then(function(){var threadId=e.detail&&e.detail.thread_id;if(threadId)openThread(threadId);}).catch(function(){});});
var requested=new URLSearchParams(location.search).get('thread');loadThreads().then(function(){if(requested)openThread(requested);}).catch(function(e){var list=host.querySelector('[data-crm-message-thread-list]');if(list)list.innerHTML='<div class="mg-empty-state"><strong>Unable to load CRM messages</strong><p>'+esc(e.message||'Try again.')+'</p></div>';});
});