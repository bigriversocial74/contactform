document.addEventListener('DOMContentLoaded', function () {
  'use strict';
  var selectedNode=document.getElementById('mg-selected-agent-id');
  var agentId=selectedNode?JSON.parse(selectedNode.textContent||'""'):'';
  if(!agentId)return;
  var root=document.querySelector('[data-agent-instance-canvas]');
  if(!root)return;
  var messages=root.querySelector('[data-agent-runtime-messages]');
  var composer=root.querySelector('[data-agent-runtime-composer]');
  var status=root.querySelector('[data-agent-runtime-status]');
  var memoryList=root.querySelector('[data-agent-memory-list]');
  var contextPanel=root.querySelector('[data-task-agent-context]');
  var contextStats=root.querySelector('[data-task-agent-stats]');
  var contextOpportunities=root.querySelector('[data-task-agent-opportunities]');
  var onboardingForm=document.querySelector('[data-agent-onboarding-form]');
  var currentThread='';
  function csrf(){var node=document.querySelector('meta[name="csrf-token"]');return node?node.content:'';}
  function esc(value){return String(value||'').replace(/[&<>"']/g,function(ch){return({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'})[ch];});}
  async function request(method,url,payload){var options={method:method,credentials:'same-origin',headers:{Accept:'application/json'}};if(payload){options.headers['Content-Type']='application/json';options.headers['X-CSRF-Token']=csrf();options.body=JSON.stringify(payload);}var response=await fetch(url,options),json=await response.json();if(!response.ok||!json.ok)throw new Error(json.message||'Unable to complete the agent request.');return json.data||json;}
  function renderCards(cards){if(!Array.isArray(cards)||!cards.length)return'';return'<div class="mg-agent-runtime-cards">'+cards.map(function(card){var action='',payload=esc(JSON.stringify(card.review_payload||{}));if(card.action==='save_draft')action='<button type="button" data-save-agent-draft data-draft-title="'+esc(card.title||'Agent draft')+'" data-draft-payload="'+payload+'">Save reviewable draft</button>';else if(card.action==='save_reminder')action='<button type="button" data-save-agent-reminder data-reminder-payload="'+payload+'">Create reminder</button>';else if(card.action==='manage_reminder')action='<div class="mg-agent-reminder-actions" data-reminder-id="'+esc((card.review_payload||{}).reminder_id||'')+'"><button type="button" data-reminder-status="completed">Complete</button><button type="button" data-reminder-status="dismissed">Dismiss</button><button type="button" data-reminder-status="cancelled">Cancel</button></div>';else if(card.action==='seed_prompt'&&card.prompt)action='<button type="button" data-agent-seed-prompt="'+esc(card.prompt)+'">Open in chat</button>';return'<article><span>'+esc(card.type||'Agent draft')+'</span><h4>'+esc(card.title||'Next step')+'</h4><p>'+esc(card.body||'')+'</p>'+action+'</article>';}).join('')+'</div>';}
  function renderMessages(items){messages.innerHTML=(items||[]).map(function(item){return'<article class="mg-agent-runtime-message is-'+esc(item.role)+'"><div><strong>'+esc(item.role==='assistant'?'Agent':'You')+'</strong><time>'+esc(item.created_at||'')+'</time></div><p>'+esc(item.body||'')+'</p>'+renderCards(item.cards||[])+'</article>';}).join('');messages.scrollTop=messages.scrollHeight;}
  function renderMemory(items){if(!memoryList)return;memoryList.innerHTML=(items&&items.length)?items.map(function(item){var value=item.value;return'<article data-agent-memory-id="'+esc(item.public_id||'')+'"><span>'+esc(item.category||'memory')+'</span><strong>'+esc(item.title||item.memory_key)+'</strong><p>'+esc(typeof value==='string'?value:JSON.stringify(value))+'</p><button type="button" data-archive-agent-memory aria-label="Archive '+esc(item.title||'memory')+'">Archive</button></article>';}).join(''):'<p>No saved memory yet.</p>';}
  function renderContext(snapshot){if(!contextPanel||!snapshot||snapshot.source!=='system'){if(contextPanel)contextPanel.hidden=true;return;}var summary=snapshot.summary||{},stats=[['Contacts',summary.contacts||0],['Upcoming',summary.upcoming_dates||0],['Missing birthdays',summary.missing_birthdays||0],['Plans',summary.active_plans||0],['Reminders',summary.scheduled_reminders||0]];contextStats.innerHTML=stats.map(function(item){return'<button type="button" data-agent-seed-prompt="'+esc(item[0]==='Missing birthdays'?'Show contacts missing birthdays.':'Show my '+item[0].toLowerCase()+'.')+'"><strong>'+esc(item[1])+'</strong><span>'+esc(item[0])+'</span></button>';}).join('');var opportunities=Array.isArray(snapshot.upcoming)?snapshot.upcoming.slice(0,6):[];contextOpportunities.innerHTML=opportunities.length?opportunities.map(function(event){var name=event.contact_name||'Contact',label=event.label||'Occasion',timing=Number(event.days_until||0)===0?'Today':Number(event.days_until||0)+' days',prompt='Help me prepare for '+name+"'s "+String(label).toLowerCase()+'.';return'<article><div><span>'+esc(label)+'</span><strong>'+esc(name)+'</strong><small>'+esc(event.event_date||'')+' · '+esc(timing)+'</small></div><button type="button" data-agent-seed-prompt="'+esc(prompt)+'">Plan</button></article>';}).join(''):'<p>No upcoming saved dates in this window.</p>';contextPanel.hidden=false;}
  async function load(){status.textContent='Loading…';try{var data=await request('GET','/api/agents/runtime.php?id='+encodeURIComponent(agentId));currentThread=data.thread&&data.thread.id?data.thread.id:'';renderMessages(data.messages||[]);renderMemory(data.memory||[]);renderContext(data.context_snapshot||null);var answers=(data.onboarding&&data.onboarding.answers)||{};Object.keys(answers).forEach(function(key){if(onboardingForm&&onboardingForm.elements[key])onboardingForm.elements[key].value=answers[key]||'';});status.textContent='';}catch(error){status.textContent=error.message;}}
  async function send(message){status.textContent='Checking Microgifter data…';var sendButton=composer.querySelector('button');sendButton.disabled=true;try{var data=await request('POST','/api/agents/runtime.php',{id:agentId,action:'chat',thread_id:currentThread,message:message});currentThread=data.thread&&data.thread.id?data.thread.id:currentThread;await load();if(data.response_source==='system_query')status.textContent='Answered from Microgifter data. No AI credits used.';}catch(error){status.textContent=error.message;}finally{sendButton.disabled=false;}}
  composer.hidden=false;composer.style.display='';composer.addEventListener('submit',function(event){event.preventDefault();var field=composer.elements.message,value=field.value.trim();if(!value)return;field.value='';send(value);});
  document.addEventListener('click',function(event){var prompt=event.target.closest('[data-agent-seed-prompt]');if(prompt){event.preventDefault();event.stopImmediatePropagation();composer.elements.message.value=prompt.getAttribute('data-agent-seed-prompt')||'';composer.elements.message.focus();return;}var memory=event.target.closest('[data-archive-agent-memory]');if(memory){event.preventDefault();event.stopImmediatePropagation();var holder=memory.closest('[data-agent-memory-id]');request('POST','/api/agents/runtime.php',{id:agentId,action:'archive_memory',memory_id:holder?holder.getAttribute('data-agent-memory-id'):''}).then(function(data){renderMemory(data.memory||[]);status.textContent='Agent memory archived. No AI credits used.';}).catch(function(error){status.textContent=error.message;});return;}var draft=event.target.closest('[data-save-agent-draft]');if(draft){event.preventDefault();event.stopImmediatePropagation();var payload={};try{payload=JSON.parse(draft.getAttribute('data-draft-payload')||'{}');}catch(e){}request('POST','/api/agents/runtime.php',{id:agentId,action:'save_draft',thread_id:currentThread,title:draft.getAttribute('data-draft-title')||'Agent draft',draft_type:'plan',payload:payload}).then(function(){status.textContent='Reviewable draft saved.';load();}).catch(function(error){status.textContent=error.message;});return;}var reminder=event.target.closest('[data-save-agent-reminder]');if(reminder){event.preventDefault();event.stopImmediatePropagation();var reminderPayload={};try{reminderPayload=JSON.parse(reminder.getAttribute('data-reminder-payload')||'{}');}catch(e){}request('POST','/api/agents/runtime.php',{id:agentId,action:'create_reminder',reminder:reminderPayload.canonical_reminder||{}}).then(function(){status.textContent='In-app reminder created. No AI credits used.';load();}).catch(function(error){status.textContent=error.message;});return;}var reminderStatus=event.target.closest('[data-reminder-status]');if(reminderStatus){event.preventDefault();event.stopImmediatePropagation();var reminderHolder=reminderStatus.closest('[data-reminder-id]');request('POST','/api/agents/runtime.php',{id:agentId,action:'update_reminder_status',reminder_id:reminderHolder?reminderHolder.getAttribute('data-reminder-id'):'',status:reminderStatus.getAttribute('data-reminder-status')||''}).then(function(){status.textContent='Reminder updated. No AI credits used.';load();}).catch(function(error){status.textContent=error.message;});}},true);
  if(onboardingForm){onboardingForm.addEventListener('submit',function(event){event.preventDefault();var form=new FormData(onboardingForm),answers={};form.forEach(function(value,key){if(key!=='settings_action')answers[key]=String(value).trim();});var action=(event.submitter&&event.submitter.value)||'save',note=onboardingForm.querySelector('[data-agent-onboarding-status]');note.textContent=action==='apply'?'Saving and applying…':'Saving…';request('POST','/api/agents/runtime.php',{id:agentId,action:'onboarding',status:'completed',current_step:'complete',answers:answers}).then(function(data){renderMemory(data.memory||[]);note.textContent=action==='apply'?'Settings saved and applied.':'Settings saved.';if(action==='apply')load();}).catch(function(error){note.textContent=error.message;});});}
  load();
});
