(function () {
  'use strict';

  var script = document.currentScript || (function () { var scripts = document.getElementsByTagName('script'); return scripts[scripts.length - 1]; })();
  function scriptOrigin() { try { return new URL(script && script.src ? script.src : '', document.baseURI).origin; } catch (error) { return 'https://microgifter.com'; } }
  var apiBase = (script && script.getAttribute('data-microgifter-api-base')) || scriptOrigin();
  var scriptDebug = !!(script && script.getAttribute('data-microgifter-debug') === '1');

  function esc(value) { return String(value == null ? '' : value).replace(/[&<>'"]/g, function (char) { return ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[char]; }); }
  function url(path) { try { return new URL(path, apiBase).toString(); } catch (error) { return String(apiBase).replace(/\/$/, '') + '/' + String(path || '').replace(/^\//, ''); } }
  function isDebug(container) { return scriptDebug || !!(container && container.getAttribute('data-microgifter-debug') === '1'); }
  function log(container, message, detail) { if (!isDebug(container) || !window.console || !console.info) return; console.info('[Microgifter Campaign Embed] ' + message, detail || ''); }
  function emit(container, name, detail) { try { container.dispatchEvent(new CustomEvent(name, { bubbles: true, detail: detail || {} })); } catch (error) {} }

  function injectBaseStyles() {
    if (document.getElementById('microgifter-campaign-embed-style')) return;
    var style = document.createElement('style');
    style.id = 'microgifter-campaign-embed-style';
    style.textContent = '.microgifter-campaign-widget{font:inherit;color:inherit;max-width:100%;}' +
      '.microgifter-campaign-widget *{box-sizing:border-box;}' +
      '.microgifter-campaign-widget article{border:1px solid rgba(120,130,150,.28);border-radius:1rem;padding:1.25rem;background:transparent;}' +
      '.microgifter-campaign-widget h3,.microgifter-campaign-widget p{margin-top:0;}' +
      '.microgifter-campaign-widget form{display:grid;gap:.75rem;margin-top:1rem;}' +
      '.microgifter-campaign-widget label{display:grid;gap:.35rem;font:inherit;}' +
      '.microgifter-campaign-widget input,.microgifter-campaign-widget textarea,.microgifter-campaign-widget select{font:inherit;width:100%;padding:.75rem;border:1px solid rgba(120,130,150,.35);border-radius:.75rem;background:transparent;color:inherit;}' +
      '.microgifter-campaign-widget button,.microgifter-campaign-widget .mg-embed-action{font:inherit;cursor:pointer;border-radius:999px;padding:.8rem 1rem;border:1px solid currentColor;background:currentColor;color:Canvas;text-decoration:none;display:inline-flex;align-items:center;justify-content:center;gap:.35rem;}' +
      '.microgifter-campaign-widget button:disabled{opacity:.62;cursor:wait;}' +
      '.microgifter-campaign-widget .mg-embed-meta{display:flex;flex-wrap:wrap;gap:.5rem;margin:.85rem 0;}' +
      '.microgifter-campaign-widget .mg-embed-meta span{border:1px solid rgba(120,130,150,.32);border-radius:999px;padding:.3rem .55rem;font-size:.82em;}' +
      '.microgifter-campaign-widget .mg-embed-result{display:none;margin-top:.75rem;padding:.75rem;border-radius:.75rem;border:1px solid rgba(120,130,150,.28);}' +
      '.microgifter-campaign-widget .mg-embed-result.is-visible{display:block;}' +
      '.microgifter-campaign-widget .mg-embed-result.is-error{border-color:rgba(190,18,60,.35);}' +
      '.microgifter-campaign-widget .mg-embed-launcher-panel[hidden]{display:none!important;}' +
      '.microgifter-campaign-widget .mg-embed-launcher-panel{margin-top:1rem;}' +
      '.microgifter-campaign-widget .mg-embed-muted{opacity:.74;}' +
      '.microgifter-campaign-widget .mg-embed-brand{font-size:.82em;opacity:.7;margin-top:.75rem;}' +
      '.microgifter-campaign-widget .mg-embed-actions{display:flex;flex-wrap:wrap;gap:.65rem;margin-top:1rem;}' +
      '.microgifter-campaign-widget .mg-embed-link{color:inherit;text-decoration:underline;text-underline-offset:.18em;}' +
      '.microgifter-campaign-widget .mg-embed-media-note{font-size:.92em;opacity:.82;}' +
      '.microgifter-campaign-widget .mg-embed-milestones{display:flex;flex-wrap:wrap;gap:.45rem;margin:.8rem 0;}' +
      '.microgifter-campaign-widget .mg-embed-milestones span{border:1px solid rgba(120,130,150,.28);border-radius:999px;padding:.28rem .52rem;font-size:.8em;}' +
      '.microgifter-campaign-widget .mg-embed-scratch,.microgifter-campaign-widget .mg-embed-stamp,.microgifter-campaign-widget .mg-embed-rsvp{border:1px dashed rgba(120,130,150,.45);border-radius:1rem;padding:1rem;text-align:center;background:rgba(120,130,150,.08);}' +
      '.microgifter-campaign-widget.is-compact article{padding:1rem;border-radius:.85rem;}' +
      '.microgifter-campaign-widget.is-compact .mg-embed-meta{display:none;}' +
      '.microgifter-campaign-widget.is-compact form{gap:.55rem;}';
    document.head.appendChild(style);
  }

  async function fetchJson(fetchUrl, options) { var response = await fetch(fetchUrl, options || {}); var requestId = response.headers.get('x-request-id') || ''; var data = await response.json().catch(function () { return {}; }); if (!response.ok || data.ok === false) { var message = data.message || data.error || 'Microgifter campaign request failed.'; if (requestId) message += ' Reference: ' + requestId; var error = new Error(message); error.status = response.status; error.requestId = requestId; error.payload = data; throw error; } if (data && data.data && typeof data.data === 'object' && !Array.isArray(data.data)) { if (data.message && data.data.message == null) data.data.message = data.message; return data.data; } return data.data || data; }
  function embedMode(container) { return container.getAttribute('data-microgifter-effective-display') || container.getAttribute('data-microgifter-display') || container.getAttribute('data-microgifter-mode') || ''; }
  function track(container, payload, eventType, metadata) { if (!payload || !payload.campaign) return; var body = JSON.stringify({ campaign_id: payload.campaign.id, campaign: payload.campaign.slug || payload.campaign.id, campaign_type: payload.campaign.campaign_type || '', event_type: eventType, embed_origin: window.location.origin || '', page_url: window.location.href || '', embed_mode: embedMode(container), embed_source: container.getAttribute('data-microgifter-source') || 'website_embed', debug: isDebug(container), metadata: metadata || {} }); var endpoint = url('/api/public/campaigns/embed-event.php'); try { if (navigator.sendBeacon) { var blob = new Blob([body], { type: 'application/json' }); if (navigator.sendBeacon(endpoint, blob)) return; } } catch (error) {} fetch(endpoint, { method: 'POST', mode: 'cors', keepalive: true, headers: { 'Accept': 'application/json', 'Content-Type': 'application/json' }, body: body }).catch(function () {}); }
  function isMediaCampaign(campaign) { return ['watch_video_reward', 'listen_music_reward'].indexOf(String((campaign || {}).campaign_type || '')) > -1; }
  function milestoneHtml(media) { var items = (media && Array.isArray(media.milestones)) ? media.milestones : []; if (!items.length && media && media.required_percent) items = [{ percent: media.required_percent, label: media.required_percent + '% milestone' }]; return items.length ? '<div class="mg-embed-milestones">' + items.slice(0, 3).map(function (item) { return '<span>' + esc(item.percent || '') + '%</span>'; }).join('') + '</div>' : ''; }
  function mediaHtml(payload) { var campaign = payload.campaign || {}, merchant = payload.merchant || {}, reward = payload.reward || {}, media = campaign.media || {}; if (!campaign.available) return '<article><p class="mg-embed-muted">' + esc(merchant.name || 'Microgifter merchant') + '</p><h3>' + esc(campaign.headline || campaign.title || 'Campaign') + '</h3><p>' + esc(campaign.availability_message || 'This campaign is not available.') + '</p><p class="mg-embed-brand">Powered by Microgifter</p></article>'; var ctaUrl = media.cta_url || campaign.public_url || '#'; var actionLabel = media.cta_label || campaign.submit_label || 'Open campaign'; var titleLine = campaign.campaign_type === 'listen_music_reward' && (media.track_title || media.artist_name) ? [media.track_title, media.artist_name].filter(Boolean).join(' · ') : (campaign.headline || campaign.title || 'Microgifter campaign'); return '<article data-microgifter-media-card><p class="mg-embed-muted">' + esc(merchant.name || 'Microgifter merchant') + '</p><h3>' + esc(titleLine) + '</h3><p>' + esc(campaign.description || 'Open this Microgifter media reward to continue.') + '</p><div class="mg-embed-meta"><span>' + esc(campaign.type_label || 'Media reward') + '</span><span>' + esc(media.provider_label || 'Media') + '</span><span>' + esc(reward.title || 'Reward') + '</span><span>Inbox + PPPM</span></div>' + milestoneHtml(media) + '<p class="mg-embed-media-note">' + esc(media.note || 'This media reward opens the full Microgifter page so progress tracking and reward delivery stay accurate.') + '</p><div class="mg-embed-actions"><a class="mg-embed-action" href="' + esc(ctaUrl) + '" target="_blank" rel="noopener" data-microgifter-media-open>' + esc(actionLabel) + '</a>' + (campaign.public_url ? '<a class="mg-embed-link" href="' + esc(campaign.public_url) + '" target="_blank" rel="noopener" data-microgifter-media-open>Open public page</a>' : '') + '</div><p class="mg-embed-brand">Powered by Microgifter</p></article>'; }
  function extraFields(campaign) { var type = campaign.campaign_type || ''; if (type === 'contest_giveaway') return '<label>Entry note<textarea name="entry_note" rows="3" placeholder="Add a short note for this contest"></textarea></label>'; if (type === 'referral_reward') return '<label>Referral note<textarea name="entry_note" rows="3" placeholder="Who referred you or who should we contact?"></textarea></label>'; if (type === 'birthday_vip') return '<label>Birthday month<select name="birthday_month"><option value="">Select month</option><option>January</option><option>February</option><option>March</option><option>April</option><option>May</option><option>June</option><option>July</option><option>August</option><option>September</option><option>October</option><option>November</option><option>December</option></select></label>'; if (type === 'agent_offer') return '<label>Offer interest<textarea name="entry_note" rows="3" placeholder="What kind of local offer are you interested in?"></textarea></label>'; if (type === 'survey_feedback_reward') return '<label>Rating<select name="entry_rating" required><option value="">Choose rating</option><option value="5">5 - Excellent</option><option value="4">4 - Good</option><option value="3">3 - Okay</option><option value="2">2 - Needs work</option><option value="1">1 - Poor</option></select></label><label>Feedback<textarea name="entry_feedback" rows="4" required minlength="3" maxlength="1200" placeholder="Share a short note with the merchant."></textarea></label>'; if (type === 'check_in_reward') return '<input type="hidden" name="entry_latitude"><input type="hidden" name="entry_longitude"><input type="hidden" name="entry_accuracy_meters"><input type="hidden" name="entry_location_permission" value="pending"><p class="mg-embed-media-note">This reward will ask for browser location access so Microgifter can match you to a registered merchant location.</p>'; if (type === 'instant_win_reward') return '<input type="hidden" name="entry_reveal_confirmed" value="0"><button type="button" class="mg-embed-scratch" data-microgifter-instant-reveal>Scratch / reveal card</button><p class="mg-embed-media-note">Reveal the card, then submit to record your instant-win play.</p>'; if (type === 'stamp_card_reward') return '<div class="mg-embed-stamp"><strong>Stamp card</strong><p class="mg-embed-media-note">Submit after each eligible visit or purchase. When the card is full, your reward is sent to Inbox.</p></div>'; if (type === 'rsvp_event_reward') return '<div class="mg-embed-rsvp"><strong>RSVP event</strong><p class="mg-embed-media-note">Submit an RSVP now, or enter the attendance code at the event to unlock the reward.</p></div><label>Attendance code <span class="mg-embed-muted">(event only)</span><input name="entry_attendance_code" maxlength="64" placeholder="Optional event code"></label>'; return ''; }
  function formHtml(payload) { var campaign = payload.campaign || {}, merchant = payload.merchant || {}, reward = payload.reward || {}; if (isMediaCampaign(campaign)) return mediaHtml(payload); if (!campaign.available) return '<article><p class="mg-embed-muted">' + esc(merchant.name || 'Microgifter merchant') + '</p><h3>' + esc(campaign.headline || campaign.title || 'Campaign') + '</h3><p>' + esc(campaign.availability_message || 'This campaign is not available.') + '</p><p class="mg-embed-brand">Powered by Microgifter</p></article>'; var fallback = campaign.campaign_type === 'check_in_reward' ? 'Check in' : (campaign.campaign_type === 'instant_win_reward' ? 'Submit instant win' : (campaign.campaign_type === 'stamp_card_reward' ? 'Add stamp' : (campaign.campaign_type === 'rsvp_event_reward' ? 'Submit RSVP' : 'Submit'))); return '<article><p class="mg-embed-muted">' + esc(merchant.name || 'Microgifter merchant') + '</p><h3>' + esc(campaign.headline || campaign.title || 'Microgifter campaign') + '</h3><p>' + esc(campaign.description || 'Enter your information below to join this campaign.') + '</p><div class="mg-embed-meta"><span>' + esc(campaign.type_label || 'Campaign') + '</span><span>' + esc(reward.title || 'Reward') + '</span><span>' + esc(reward.value_label || 'Reward') + '</span></div><form data-microgifter-embed-form novalidate><input type="hidden" name="campaign_id" value="' + esc(campaign.id || '') + '"><input type="hidden" name="campaign" value="' + esc(campaign.slug || campaign.id || '') + '"><input type="hidden" name="campaign_type" value="' + esc(campaign.campaign_type || '') + '">' + (campaign.qr_token ? '<input type="hidden" name="qr_token" value="' + esc(campaign.qr_token) + '">' : '') + '<label>Name<input name="name" autocomplete="name" maxlength="180" placeholder="Your name"></label><label>Email<input name="email" type="email" autocomplete="email" maxlength="255" required placeholder="you@example.com"></label><label>Phone<input name="phone" type="tel" autocomplete="tel" maxlength="60" placeholder="Optional"></label>' + extraFields(campaign) + '<button type="submit">' + esc(campaign.submit_label || fallback) + '</button></form><div class="mg-embed-result" data-microgifter-embed-result></div><p class="mg-embed-brand">Powered by Microgifter</p></article>'; }
  function validEmail(value) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(value || '').trim()); }
  function payloadFromForm(form, campaign, container) { var formData = new FormData(form), payload = {}; formData.forEach(function (value, key) { payload[key] = String(value || '').trim(); }); var entry = {}; Object.keys(payload).forEach(function (key) { if (key.indexOf('entry_') !== 0) return; entry[key.replace(/^entry_/, '')] = payload[key]; delete payload[key]; }); if (payload.entry_note) entry.note = payload.entry_note; if (payload.birthday_month) entry.birthday_month = payload.birthday_month; delete payload.entry_note; delete payload.birthday_month; if (Object.keys(entry).length) payload.entry = entry; payload.campaign_type = payload.campaign_type || campaign.campaign_type || ''; payload.embed_source = container.getAttribute('data-microgifter-source') || 'website_embed'; payload.embed_origin = window.location.origin || ''; payload.page_url = window.location.href || ''; payload.embed_mode = embedMode(container); return payload; }
  function setResult(mount, message, isError, data) { var result = mount.querySelector('[data-microgifter-embed-result]'); if (!result) return; data = data || {}; result.classList.add('is-visible'); result.classList.toggle('is-error', !!isError); result.setAttribute('role', isError ? 'alert' : 'status'); if (!isError && data.rsvp_status === 'rsvped' && !data.wallet_item_id) { result.innerHTML = '<strong>RSVP recorded</strong><p>Attendance reward eligibility will be checked at the event.</p>'; return; } if (!isError && data.instant_win_result === 'not_won') { result.innerHTML = '<strong>' + esc(data.message || message || 'Not a winner this time') + '</strong><p>Your play was recorded with the merchant.</p>'; return; } if (!isError && data.stamp_result === 'recorded' && !data.wallet_item_id) { result.innerHTML = '<strong>' + esc(message || 'Stamp recorded') + '</strong><p>' + esc(String(data.stamp_count || 0)) + ' of ' + esc(String(data.required_count || 0)) + ' stamps recorded. ' + esc(String(data.stamps_remaining || 0)) + ' remaining.</p>'; return; } if (!isError && (data.wallet_item_id || data.pppm_bridge || data.inbox_url)) { var inboxUrl = data.inbox_url || '/inbox.php'; var location = data.entry && data.entry.matched_location ? '<p>Matched location: ' + esc(data.entry.matched_location.name || '') + '</p>' : ''; result.innerHTML = '<strong>Reward sent to your Microgifter Inbox</strong>' + location + '<p>Open your Microgifter Inbox to view, manage, claim, redeem, or continue the PPPM flow for this reward.</p><div class="mg-embed-actions"><a class="mg-embed-action" href="' + esc(inboxUrl) + '">Open Microgifter Inbox</a></div>'; return; } result.textContent = message || (isError ? 'Unable to submit campaign response.' : 'Campaign response submitted.'); }
  function captureLocation(form) { if (!navigator.geolocation) return Promise.reject(new Error('This browser does not support location check-in.')); return new Promise(function (resolve, reject) { navigator.geolocation.getCurrentPosition(function (position) { var coords = position.coords || {}; if (form.elements.entry_latitude) form.elements.entry_latitude.value = coords.latitude; if (form.elements.entry_longitude) form.elements.entry_longitude.value = coords.longitude; if (form.elements.entry_accuracy_meters) form.elements.entry_accuracy_meters.value = coords.accuracy || ''; if (form.elements.entry_location_permission) form.elements.entry_location_permission.value = 'granted'; resolve(position); }, function () { if (form.elements.entry_location_permission) form.elements.entry_location_permission.value = 'denied'; reject(new Error('Location permission is required for this check-in reward.')); }, { enableHighAccuracy: true, timeout: 12000, maximumAge: 30000 }); }); }
  function bindSubmit(mount, payload) { var form = mount.querySelector('[data-microgifter-embed-form]'); if (!form) return; var reveal = form.querySelector('[data-microgifter-instant-reveal]'); if (reveal) reveal.addEventListener('click', function () { if (form.elements.entry_reveal_confirmed) form.elements.entry_reveal_confirmed.value = '1'; reveal.disabled = true; reveal.textContent = 'Revealed'; }); form.addEventListener('submit', async function (event) { event.preventDefault(); var campaign = payload.campaign || {}; if (isMediaCampaign(campaign)) { setResult(mount, 'Open the full media reward page to continue.', true); return; } var button = form.querySelector('button[type="submit"]'); if (button) { button.disabled = true; button.textContent = campaign.campaign_type === 'check_in_reward' ? 'Checking in...' : (campaign.campaign_type === 'instant_win_reward' ? 'Revealing...' : (campaign.campaign_type === 'stamp_card_reward' ? 'Recording stamp...' : (campaign.campaign_type === 'rsvp_event_reward' ? 'Submitting RSVP...' : 'Sending...'))); } try { if (campaign.campaign_type === 'check_in_reward') await captureLocation(form); if (campaign.campaign_type === 'instant_win_reward' && form.elements.entry_reveal_confirmed && form.elements.entry_reveal_confirmed.value !== '1') throw new Error('Reveal the instant win card before submitting.'); var prepared = payloadFromForm(form, campaign, mount); if (!validEmail(prepared.email)) { setResult(mount, 'Enter a valid email address to continue.', true); emit(mount, 'microgifter:campaignEmbedInvalid', { reason: 'email' }); track(mount, payload, 'invalid', { reason: 'email' }); return; } var invalid = Array.prototype.slice.call(form.elements || []).find(function (field) { return field && typeof field.checkValidity === 'function' && !field.checkValidity(); }); if (invalid) { if (typeof invalid.reportValidity === 'function') invalid.reportValidity(); setResult(mount, 'Complete the required fields to continue.', true); return; } log(mount, 'Submitting campaign embed form.', { campaign: campaign.id, type: campaign.campaign_type }); var result = await fetchJson(campaign.submit_endpoint || url('/api/public/campaigns/embed-submit.php'), { method: 'POST', mode: 'cors', headers: { 'Accept': 'application/json', 'Content-Type': 'application/json' }, body: JSON.stringify(prepared) }); setResult(mount, (result && result.message) || campaign.success_message || 'Campaign response submitted.', false, result); emit(mount, 'microgifter:campaignEmbedSubmitted', { campaign: campaign.id, response: result }); track(mount, payload, 'submitted', { ok: true }); form.reset(); } catch (error) { setResult(mount, error.message || 'Unable to submit campaign response.', true); emit(mount, 'microgifter:campaignEmbedError', { stage: 'submit', error: error }); track(mount, payload, 'error', { stage: 'submit', status: error.status || null }); log(mount, 'Submit failed.', error); } finally { if (button) { button.disabled = false; button.textContent = (payload.campaign && payload.campaign.submit_label) || (campaign.campaign_type === 'check_in_reward' ? 'Check in' : (campaign.campaign_type === 'instant_win_reward' ? 'Submit instant win' : (campaign.campaign_type === 'stamp_card_reward' ? 'Add stamp' : (campaign.campaign_type === 'rsvp_event_reward' ? 'Submit RSVP' : 'Submit')))); } } }); }
  function bindMediaActions(mount, payload) { if (!payload || !isMediaCampaign(payload.campaign || {})) return; mount.querySelectorAll('[data-microgifter-media-open]').forEach(function (link) { link.addEventListener('click', function () { emit(mount, 'microgifter:campaignEmbedOpened', { campaign: payload.campaign, media: (payload.campaign || {}).media || null }); track(mount, payload, 'opened', { mode: embedMode(mount), media_handoff: true }); }); }); }
  function effectiveDisplay(container, payload) { var settings = payload && payload.embed && payload.embed.settings ? payload.embed.settings : {}; return container.getAttribute('data-microgifter-display') || container.getAttribute('data-microgifter-mode') || settings.default_layout || 'inline'; }
  async function renderEmbed(container) { if (!container || container.getAttribute('data-microgifter-rendered') === '1') return; var campaignRef = container.getAttribute('data-microgifter-campaign') || container.getAttribute('data-campaign') || ''; if (!campaignRef) { injectBaseStyles(); container.classList.add('microgifter-campaign-widget'); container.innerHTML = '<article><h3>Campaign unavailable</h3><p>Missing Microgifter campaign reference.</p></article>'; emit(container, 'microgifter:campaignEmbedError', { stage: 'boot', message: 'missing_campaign_reference' }); return; } container.setAttribute('data-microgifter-rendered', '1'); injectBaseStyles(); container.classList.add('microgifter-campaign-widget'); container.innerHTML = '<article><p>Loading Microgifter campaign...</p></article>'; try { log(container, 'Loading campaign embed payload.', { campaign: campaignRef, base: apiBase }); var payload = await fetchJson(url('/api/public/campaigns/embed.php?campaign=' + encodeURIComponent(campaignRef)), { mode: 'cors', headers: { 'Accept': 'application/json' } }); var display = effectiveDisplay(container, payload); container.setAttribute('data-microgifter-effective-display', display); container.classList.toggle('is-compact', display === 'compact'); if (display === 'button' || display === 'popup') { var buttonText = container.getAttribute('data-microgifter-button-label') || ((payload.campaign && payload.campaign.submit_label) || 'Open campaign'); container.innerHTML = '<button type="button" data-microgifter-embed-launcher>' + esc(buttonText) + '</button><div class="mg-embed-launcher-panel" data-microgifter-embed-panel hidden>' + formHtml(payload) + '</div>'; var launcher = container.querySelector('[data-microgifter-embed-launcher]'); var panel = container.querySelector('[data-microgifter-embed-panel]'); if (launcher && panel) launcher.addEventListener('click', function () { panel.hidden = !panel.hidden; if (!panel.hidden) track(container, payload, 'opened', { mode: display }); }); } else { container.innerHTML = formHtml(payload); } bindSubmit(container, payload); bindMediaActions(container, payload); emit(container, 'microgifter:campaignEmbedLoaded', { campaign: payload.campaign, merchant: payload.merchant, reward: payload.reward }); track(container, payload, 'loaded', { mode: display, media_handoff: isMediaCampaign(payload.campaign || {}) }); log(container, 'Campaign embed loaded.', payload.campaign || {}); } catch (error) { container.innerHTML = '<article><h3>Campaign unavailable</h3><p>' + esc(error.message || 'This Microgifter campaign could not be loaded.') + '</p></article>'; emit(container, 'microgifter:campaignEmbedError', { stage: 'load', error: error }); log(container, 'Load failed.', error); } }
  function boot() { Array.prototype.slice.call(document.querySelectorAll('[data-microgifter-campaign]')).forEach(renderEmbed); }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot, { once: true }); else boot();
})();