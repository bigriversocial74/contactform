(() => {
  'use strict';
  const desktop = matchMedia('(min-width: 901px)');
  const reduced = matchMedia('(prefers-reduced-motion: reduce)');
  const states = {AL:'Alabama',AK:'Alaska',AZ:'Arizona',AR:'Arkansas',CA:'California',CO:'Colorado',CT:'Connecticut',DE:'Delaware',FL:'Florida',GA:'Georgia',HI:'Hawaii',ID:'Idaho',IL:'Illinois',IN:'Indiana',IA:'Iowa',KS:'Kansas',KY:'Kentucky',LA:'Louisiana',ME:'Maine',MD:'Maryland',MA:'Massachusetts',MI:'Michigan',MN:'Minnesota',MS:'Mississippi',MO:'Missouri',MT:'Montana',NE:'Nebraska',NV:'Nevada',NH:'New Hampshire',NJ:'New Jersey',NM:'New Mexico',NY:'New York',NC:'North Carolina',ND:'North Dakota',OH:'Ohio',OK:'Oklahoma',OR:'Oregon',PA:'Pennsylvania',RI:'Rhode Island',SC:'South Carolina',SD:'South Dakota',TN:'Tennessee',TX:'Texas',UT:'Utah',VT:'Vermont',VA:'Virginia',WA:'Washington',WV:'West Virginia',WI:'Wisconsin',WY:'Wyoming'};
  const nameToCode = Object.fromEntries(Object.entries(states).map(([code, name]) => [name.toLowerCase(), code]));
  const leafletCss = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
  const leafletJs = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';
  const geoJson = 'https://cdn.jsdelivr.net/gh/PublicaMundi/MappingAPI@master/data/geojson/us-states.json';
  const countUrl = '/api/public/location-counts.php';
  const css = `@media (min-width:901px){body.mg-home-sticky-map-enabled{overflow-x:clip}body.mg-home-sticky-map-enabled .mg-index-hero{display:block;min-height:520svh;align-items:initial;overflow:visible;border-bottom:0;background-attachment:fixed;isolation:isolate}body.mg-home-sticky-map-enabled .mg-index-hero:after{content:"";position:absolute;left:0;right:0;bottom:-1px;height:36vh;z-index:0;pointer-events:none;background:linear-gradient(180deg,rgba(238,242,247,0),rgba(238,242,247,.96))}body.mg-home-sticky-map-enabled .mg-index-hero-inner{position:sticky;top:64px;width:100%;max-width:none;margin:0;padding:0 6vw;box-sizing:border-box;height:calc(100svh - 64px);min-height:calc(100svh - 64px);display:grid;grid-template-columns:.9fr 1.1fr;align-items:center;overflow:visible;z-index:2;transform:none!important}body.mg-home-sticky-map-enabled .mg-home-pretitle{position:absolute;left:6vw;top:clamp(92px,14vh,150px);z-index:4;display:inline-flex;align-items:center;min-height:34px;padding:0 14px;border:1px solid rgba(124,58,237,.2);border-radius:999px;background:rgba(255,255,255,.86);box-shadow:0 18px 48px rgba(15,23,42,.1);backdrop-filter:blur(14px);color:#7c3aed;font-size:clamp(12px,1.04vw,15px);font-weight:950;letter-spacing:.105em;text-transform:uppercase;white-space:nowrap;pointer-events:none;will-change:transform,opacity,filter;transform:translate3d(var(--mg-home-pre-x,-58vw),var(--mg-home-pre-y,0),0) scale(var(--mg-home-pre-scale,1));opacity:var(--mg-home-pre-opacity,0);filter:blur(var(--mg-home-pre-blur,0px))}body.mg-home-sticky-map-enabled .mg-index-hero-copy,body.mg-home-sticky-map-enabled .mg-index-hero-visual{will-change:transform,opacity,filter;transition:filter .12s linear}body.mg-home-sticky-map-enabled .mg-index-hero-copy{transform:translate3d(var(--mg-home-copy-x,0),var(--mg-home-panel-y,0),0) scale(var(--mg-home-panel-scale,1));transform-origin:50% 42%;opacity:var(--mg-home-panel-opacity,1);filter:blur(var(--mg-home-panel-blur,0px))}body.mg-home-sticky-map-enabled .mg-index-hero-visual{transform:translate3d(var(--mg-home-visual-x,0),var(--mg-home-panel-y,0),0) scale(var(--mg-home-panel-scale,1));transform-origin:50% 42%;opacity:var(--mg-home-panel-opacity,1);filter:blur(var(--mg-home-panel-blur,0px))}body.mg-home-sticky-map-enabled .mg-index-hero.is-map-clickable .mg-index-hero-copy,body.mg-home-sticky-map-enabled .mg-index-hero.is-map-clickable .mg-index-hero-visual{pointer-events:none}body.mg-home-sticky-map-enabled .mg-home-usa-map-stage{position:absolute;inset:0;z-index:5;display:grid;place-items:center;pointer-events:none;opacity:var(--mg-home-map,0);transform:translate3d(0,var(--mg-home-map-y,32px),0) scale(var(--mg-home-map-scale,.94));will-change:opacity,transform}body.mg-home-sticky-map-enabled .mg-index-hero.is-map-clickable .mg-home-usa-map-stage{pointer-events:auto}body.mg-home-sticky-map-enabled .mg-home-usa-map-shell{width:min(1160px,92vw);padding:24px;border:1px solid rgba(124,58,237,.16);border-radius:32px;background:rgba(255,255,255,.9);box-shadow:0 34px 100px rgba(15,23,42,.16);backdrop-filter:blur(18px)}body.mg-home-sticky-map-enabled .mg-home-usa-map-header{display:flex;align-items:flex-end;justify-content:space-between;gap:24px;margin-bottom:18px}body.mg-home-sticky-map-enabled .mg-home-usa-map-kicker{display:inline-flex;align-items:center;gap:8px;min-height:30px;padding:0 12px;border:1px solid #d8e4f4;border-radius:999px;background:#fff;color:#7c3aed;font-size:12px;font-weight:950;letter-spacing:.06em;text-transform:uppercase}body.mg-home-sticky-map-enabled .mg-home-usa-map-title{margin:10px 0 0;color:#071225;font-size:clamp(34px,3.7vw,58px);line-height:.98;letter-spacing:-.065em}body.mg-home-sticky-map-enabled .mg-home-usa-map-note{max-width:320px;margin:0;color:#64748b;font-size:13px;line-height:1.45;font-weight:750}body.mg-home-sticky-map-enabled .mg-home-leaflet-frame{position:relative;height:min(56vh,520px);min-height:390px;overflow:hidden;border:1px solid #e2e8f0;border-radius:24px;background:#eef3f8}body.mg-home-sticky-map-enabled .mg-home-leaflet-map{position:absolute;inset:0;width:100%;height:100%;background:#eef3f8}body.mg-home-sticky-map-enabled .mg-home-leaflet-frame .leaflet-container{font:inherit;background:#eef3f8}body.mg-home-sticky-map-enabled .mg-home-leaflet-frame .leaflet-control-attribution{font-size:10px}body.mg-home-sticky-map-enabled .mg-home-state-tooltip{padding:8px 10px;border:0;border-radius:10px;background:#071225;color:#fff;box-shadow:0 10px 22px rgba(15,23,42,.18);font-size:12px;font-weight:850;line-height:1.25}body.mg-home-sticky-map-enabled .mg-home-state-tooltip span{display:block;margin-top:3px;color:#cbd5e1;font-size:11px;font-weight:800}body.mg-home-sticky-map-enabled .mg-index-section,body.mg-home-sticky-map-enabled .mg-account-cta-section,body.mg-home-sticky-map-enabled .mg-home-footer{position:relative;z-index:3}}@media (max-width:900px),(prefers-reduced-motion:reduce){.mg-home-usa-map-stage,.mg-home-pretitle{display:none!important}}`;
  let hero = null, enabled = false, ticking = false, requested = false, mapInstance = null, geoLayer = null, counts = {}, countPromise = null;
  const clamp = (value, min, max) => Math.min(Math.max(value, min), max);
  const smooth = (start, end, value) => { const x = clamp((value - start) / (end - start), 0, 1); return x * x * (3 - (2 * x)); };
  const ensureStyle = () => { if (document.getElementById('mg-home-sticky-usa-map-style')) return; const style = document.createElement('style'); style.id = 'mg-home-sticky-usa-map-style'; style.textContent = css; document.head.appendChild(style); };
  const ensureCss = () => { if (document.querySelector(`link[href="${leafletCss}"]`)) return; const link = document.createElement('link'); link.rel = 'stylesheet'; link.href = leafletCss; document.head.appendChild(link); };
  const ensureJs = () => new Promise((resolve, reject) => { if (typeof L !== 'undefined') return resolve(L); const existing = document.querySelector(`script[src="${leafletJs}"]`); if (existing) { existing.addEventListener('load', () => resolve(L), { once:true }); existing.addEventListener('error', reject, { once:true }); return; } const script = document.createElement('script'); script.src = leafletJs; script.async = true; script.onload = () => resolve(L); script.onerror = reject; document.head.appendChild(script); });
  const loadCounts = () => { if (countPromise) return countPromise; countPromise = fetch(countUrl, { headers:{ Accept:'application/json' }, credentials:'same-origin' }).then(r => { if (!r.ok) throw new Error('counts'); return r.json(); }).then(payload => (counts = payload?.data?.counts || payload?.counts || {})).catch(() => (counts = {})); return countPromise; };
  const merchantLabel = code => { const count = Number(counts[code] || 0); return `${count} merchant${count === 1 ? '' : 's'}`; };
  const stateStyle = code => Number(counts[code] || 0) > 0 ? { color:'#7c3aed', weight:1.45, fillColor:'#ddd6fe', fillOpacity:.94 } : { color:'#c9d4e2', weight:1.1, fillColor:'#e5ebf3', fillOpacity:.76 };
  const openState = code => { if (code && states[code]) location.assign(`/location-results.php?state=${encodeURIComponent(code)}`); };
  const buildPreTitle = () => { const item = document.createElement('div'); item.className = 'mg-home-pretitle'; item.textContent = 'The Easiest Way To'; return item; };
  const buildMapStage = () => { const stage = document.createElement('div'); stage.className = 'mg-home-usa-map-stage'; stage.setAttribute('aria-label', 'Choose a state to explore local gifts'); stage.innerHTML = `<div class="mg-home-usa-map-shell"><div class="mg-home-usa-map-header"><div><span class="mg-home-usa-map-kicker">Choose your market</span><h2 class="mg-home-usa-map-title">Explore local gifts by state.</h2></div><p class="mg-home-usa-map-note" data-home-map-note>Live merchant counts load into the map. Click a state to open its results page, or keep scrolling to continue.</p></div><div class="mg-home-leaflet-frame"><div class="mg-home-leaflet-map" id="mgHomeLeafletMap" aria-label="Interactive United States map"></div></div></div>`; return stage; };
  const initMap = () => {
    if (requested || mapInstance || !enabled) return;
    requested = true;
    ensureCss();
    Promise.all([ensureJs(), loadCounts(), fetch(geoJson).then(r => { if (!r.ok) throw new Error('geojson'); return r.json(); })]).then(([Leaflet, liveCounts, data]) => {
      counts = liveCounts || {};
      const el = document.getElementById('mgHomeLeafletMap');
      if (!el || mapInstance || !enabled) return;
      const bounds = Leaflet.latLngBounds([24.3, -125.0], [49.7, -66.5]);
      mapInstance = Leaflet.map(el, { zoomControl:false, dragging:true, minZoom:4, maxZoom:8, scrollWheelZoom:false, doubleClickZoom:false, boxZoom:false, keyboard:true, maxBounds:bounds.pad(.08), maxBoundsViscosity:.85 });
      mapInstance.fitBounds(bounds, { padding:[18,18] });
      Leaflet.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', { maxZoom:19, attribution:'&copy; OpenStreetMap contributors' }).addTo(mapInstance);
      const hover = { color:'#6d28d9', weight:2, fillColor:'#c4b5fd', fillOpacity:.98 };
      geoLayer = Leaflet.geoJSON(data, { style:feature => { const name = String(feature?.properties?.name || feature?.properties?.NAME || '').trim(); return stateStyle(nameToCode[name.toLowerCase()]); }, onEachFeature:(feature, layer) => { const name = String(feature?.properties?.name || feature?.properties?.NAME || '').trim(), code = nameToCode[name.toLowerCase()]; layer.bindTooltip(`<strong>${name}</strong><span>${merchantLabel(code)}</span>`, { sticky:true, direction:'top', className:'mg-home-state-tooltip' }); layer.on({ mouseover:() => { layer.setStyle(hover); layer.bringToFront(); }, mouseout:() => { if (geoLayer) geoLayer.resetStyle(layer); }, click:event => { if (event?.originalEvent) Leaflet.DomEvent.stop(event.originalEvent); openState(code); } }); } }).addTo(mapInstance);
      mapInstance.fitBounds(bounds, { padding:[18,18] });
      requestAnimationFrame(() => mapInstance.invalidateSize());
    }).catch(() => { const note = document.querySelector('[data-home-map-note]'); if (note) note.textContent = 'The state map could not load, but the Locations page remains available.'; });
  };
  const update = () => {
    ticking = false;
    if (!enabled || !hero) return;
    const top = hero.getBoundingClientRect().top + scrollY;
    const range = Math.max(hero.offsetHeight - innerHeight, 1);
    const progress = clamp((scrollY - top) / range, 0, 1);
    const preIn = smooth(.035, .13, progress);
    const preBounce = smooth(.255, .31, progress);
    const preExit = smooth(.31, .47, progress);
    const split = smooth(.34, .56, progress);
    const map = smooth(.54, .78, progress);
    const hold = smooth(.78, .92, progress);
    const opacity = Math.max(0, 1 - split * 1.08);
    const preX = (-58 * (1 - preIn)) + (preBounce * 18) + (preExit * 118);
    const preY = (-4 * preIn) - (preExit * 72);
    const preOpacity = Math.max(0, Math.min(1, preIn * 1.1, 1 - preExit * 1.12));
    hero.style.setProperty('--mg-home-pre-x', `${preX.toFixed(2)}vw`);
    hero.style.setProperty('--mg-home-pre-y', `${preY.toFixed(2)}px`);
    hero.style.setProperty('--mg-home-pre-scale', (0.96 + preIn * .04 + preExit * .14).toFixed(3));
    hero.style.setProperty('--mg-home-pre-opacity', preOpacity.toFixed(3));
    hero.style.setProperty('--mg-home-pre-blur', `${(preExit * 6).toFixed(2)}px`);
    hero.style.setProperty('--mg-home-copy-x', `${(split * -110).toFixed(2)}vw`);
    hero.style.setProperty('--mg-home-visual-x', `${(split * 110).toFixed(2)}vw`);
    hero.style.setProperty('--mg-home-panel-y', `${(split * -92).toFixed(2)}px`);
    hero.style.setProperty('--mg-home-panel-scale', (1 + split * .16).toFixed(3));
    hero.style.setProperty('--mg-home-panel-opacity', opacity.toFixed(3));
    hero.style.setProperty('--mg-home-panel-blur', `${(split * 8).toFixed(2)}px`);
    hero.style.setProperty('--mg-home-map', map.toFixed(3));
    hero.style.setProperty('--mg-home-map-y', `${((1 - map) * 34 - hold * 8).toFixed(2)}px`);
    hero.style.setProperty('--mg-home-map-scale', (0.935 + map * .065).toFixed(3));
    hero.classList.toggle('is-map-clickable', map > .72);
    if (map > .08) { initMap(); if (mapInstance) requestAnimationFrame(() => mapInstance.invalidateSize()); }
  };
  const requestUpdate = () => { if (ticking) return; ticking = true; requestAnimationFrame(update); };
  const enable = () => { if (enabled || !desktop.matches || reduced.matches) return; hero = document.querySelector('.mg-index-hero'); const inner = hero ? hero.querySelector('.mg-index-hero-inner') : null; if (!hero || !inner) return; ensureStyle(); if (!hero.querySelector('.mg-home-pretitle')) inner.appendChild(buildPreTitle()); if (!hero.querySelector('.mg-home-usa-map-stage')) inner.appendChild(buildMapStage()); enabled = true; document.body.classList.add('mg-home-sticky-map-enabled'); addEventListener('scroll', requestUpdate, { passive:true }); addEventListener('resize', requestUpdate); requestUpdate(); };
  const disable = () => { if (!enabled) return; enabled = false; document.body.classList.remove('mg-home-sticky-map-enabled'); if (hero) { hero.classList.remove('is-map-clickable'); ['--mg-home-pre-x','--mg-home-pre-y','--mg-home-pre-scale','--mg-home-pre-opacity','--mg-home-pre-blur','--mg-home-copy-x','--mg-home-visual-x','--mg-home-panel-y','--mg-home-panel-scale','--mg-home-panel-opacity','--mg-home-panel-blur','--mg-home-map','--mg-home-map-y','--mg-home-map-scale'].forEach(prop => hero.style.removeProperty(prop)); } if (mapInstance) { mapInstance.remove(); mapInstance = null; geoLayer = null; requested = false; } removeEventListener('scroll', requestUpdate); removeEventListener('resize', requestUpdate); };
  const sync = () => desktop.matches && !reduced.matches ? enable() : disable();
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', sync, { once:true }); else sync();
  if (typeof desktop.addEventListener === 'function') { desktop.addEventListener('change', sync); reduced.addEventListener('change', sync); } else { desktop.addListener(sync); reduced.addListener(sync); }
})();
