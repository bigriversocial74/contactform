name: Profile Discovery Validation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/recovery-baseline.yml

# MG_RUN_PROFILE_DISCOVERY_BEHAVIOR
# composer test-profile-discovery-behavior
# ProductionProfileDiscoverySearchFoundationTest
# profile-discovery-search-foundation.spec.js
# build_full_upgrade_sql.php
# composer test-frontend-contracts
# composer test
# npm run test:browser
