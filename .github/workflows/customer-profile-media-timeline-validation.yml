name: Customer Profile Media Timeline

on:
  pull_request:
    paths:
      - 'includes/merchant-crm.php'
      - 'merchant-customer.php'
      - 'assets/js/merchant-customer-timeline-milestones.js'
      - 'assets/css/merchant-customer-profile-navigation.css'
      - 'scripts/validate_customer_profile_media_timeline.php'
      - '.github/workflows/customer-profile-media-timeline-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.2', '8.3']
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: PHP syntax
        run: |
          php -l includes/merchant-crm.php
          php -l merchant-customer.php
          php -l scripts/validate_customer_profile_media_timeline.php
      - name: JavaScript syntax
        run: node --check assets/js/merchant-customer-timeline-milestones.js
      - name: Contract validation
        run: php scripts/validate_customer_profile_media_timeline.php
