name: Recovery Contract Modernization Archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The temporary recovery-contract modernization pass is complete.'
