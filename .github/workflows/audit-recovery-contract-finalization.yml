name: Recovery Contract Finalization Archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The temporary recovery finalization pass is complete.'
